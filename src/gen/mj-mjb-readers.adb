--  GENERATED by tools/gen.py from tools/fields.txt and tools/layout.txt. Do not edit.

with Interfaces; use Interfaces;
with MJ.Bytes;   use MJ.Bytes;

package body MJ.MJB.Readers with SPARK_Mode is

   procedure Read_Sizes (B : Byte_Array; Pos : in out Natural; S : out Sizes; Result : out Load_Result) is
      P : constant Natural := Pos;
      V : Int64;
   begin
      S := (others => <>);
      Result := OK_Result;
      V := Get_I64 (B, P + 0);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 0);
         return;
      end if;
      S.Nq := Integer (V);
      V := Get_I64 (B, P + 8);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 1);
         return;
      end if;
      S.Nv := Integer (V);
      V := Get_I64 (B, P + 16);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 2);
         return;
      end if;
      S.Nu := Integer (V);
      V := Get_I64 (B, P + 24);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 3);
         return;
      end if;
      S.Nactuator := Integer (V);
      V := Get_I64 (B, P + 32);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 4);
         return;
      end if;
      S.Nout := Integer (V);
      V := Get_I64 (B, P + 40);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 5);
         return;
      end if;
      S.Na := Integer (V);
      V := Get_I64 (B, P + 48);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 6);
         return;
      end if;
      S.Nbody := Integer (V);
      V := Get_I64 (B, P + 56);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 7);
         return;
      end if;
      S.Nbvh := Integer (V);
      V := Get_I64 (B, P + 64);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 8);
         return;
      end if;
      S.Nbvhstatic := Integer (V);
      V := Get_I64 (B, P + 72);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 9);
         return;
      end if;
      S.Nbvhdynamic := Integer (V);
      V := Get_I64 (B, P + 80);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 10);
         return;
      end if;
      S.Noct := Integer (V);
      V := Get_I64 (B, P + 88);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 11);
         return;
      end if;
      S.Njnt := Integer (V);
      V := Get_I64 (B, P + 96);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 12);
         return;
      end if;
      S.Ntree := Integer (V);
      V := Get_I64 (B, P + 104);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 13);
         return;
      end if;
      S.NM := Integer (V);
      V := Get_I64 (B, P + 112);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 14);
         return;
      end if;
      S.NB := Integer (V);
      V := Get_I64 (B, P + 120);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 15);
         return;
      end if;
      S.NC := Integer (V);
      V := Get_I64 (B, P + 128);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 16);
         return;
      end if;
      S.ND := Integer (V);
      V := Get_I64 (B, P + 136);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 17);
         return;
      end if;
      S.Ngeom := Integer (V);
      V := Get_I64 (B, P + 144);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 18);
         return;
      end if;
      S.Nsite := Integer (V);
      V := Get_I64 (B, P + 152);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 19);
         return;
      end if;
      S.Ncam := Integer (V);
      V := Get_I64 (B, P + 160);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 20);
         return;
      end if;
      S.Nlight := Integer (V);
      V := Get_I64 (B, P + 168);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 21);
         return;
      end if;
      S.Nflex := Integer (V);
      V := Get_I64 (B, P + 176);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 22);
         return;
      end if;
      S.Nflexnode := Integer (V);
      V := Get_I64 (B, P + 184);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 23);
         return;
      end if;
      S.Nflexvert := Integer (V);
      V := Get_I64 (B, P + 192);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 24);
         return;
      end if;
      S.Nflexedge := Integer (V);
      V := Get_I64 (B, P + 200);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 25);
         return;
      end if;
      S.Nflexelem := Integer (V);
      V := Get_I64 (B, P + 208);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 26);
         return;
      end if;
      S.Nflexelemdata := Integer (V);
      V := Get_I64 (B, P + 216);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 27);
         return;
      end if;
      S.Nflexstiffness := Integer (V);
      V := Get_I64 (B, P + 224);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 28);
         return;
      end if;
      S.Nflexbending := Integer (V);
      V := Get_I64 (B, P + 232);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 29);
         return;
      end if;
      S.Nefm0dof := Integer (V);
      V := Get_I64 (B, P + 240);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 30);
         return;
      end if;
      S.Nefm0L := Integer (V);
      V := Get_I64 (B, P + 248);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 31);
         return;
      end if;
      S.Nflexelemedge := Integer (V);
      V := Get_I64 (B, P + 256);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 32);
         return;
      end if;
      S.Nflexshelldata := Integer (V);
      V := Get_I64 (B, P + 264);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 33);
         return;
      end if;
      S.Nflexevpair := Integer (V);
      V := Get_I64 (B, P + 272);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 34);
         return;
      end if;
      S.Nflextexcoord := Integer (V);
      V := Get_I64 (B, P + 280);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 35);
         return;
      end if;
      S.NJfe := Integer (V);
      V := Get_I64 (B, P + 288);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 36);
         return;
      end if;
      S.NJfv := Integer (V);
      V := Get_I64 (B, P + 296);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 37);
         return;
      end if;
      S.Nmesh := Integer (V);
      V := Get_I64 (B, P + 304);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 38);
         return;
      end if;
      S.Nmeshvert := Integer (V);
      V := Get_I64 (B, P + 312);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 39);
         return;
      end if;
      S.Nmeshnormal := Integer (V);
      V := Get_I64 (B, P + 320);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 40);
         return;
      end if;
      S.Nmeshtexcoord := Integer (V);
      V := Get_I64 (B, P + 328);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 41);
         return;
      end if;
      S.Nmeshface := Integer (V);
      V := Get_I64 (B, P + 336);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 42);
         return;
      end if;
      S.Nmeshgraph := Integer (V);
      V := Get_I64 (B, P + 344);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 43);
         return;
      end if;
      S.Nmeshpoly := Integer (V);
      V := Get_I64 (B, P + 352);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 44);
         return;
      end if;
      S.Nmeshpolyvert := Integer (V);
      V := Get_I64 (B, P + 360);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 45);
         return;
      end if;
      S.Nmeshpolymap := Integer (V);
      V := Get_I64 (B, P + 368);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 46);
         return;
      end if;
      S.Nskin := Integer (V);
      V := Get_I64 (B, P + 376);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 47);
         return;
      end if;
      S.Nskinvert := Integer (V);
      V := Get_I64 (B, P + 384);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 48);
         return;
      end if;
      S.Nskintexvert := Integer (V);
      V := Get_I64 (B, P + 392);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 49);
         return;
      end if;
      S.Nskinface := Integer (V);
      V := Get_I64 (B, P + 400);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 50);
         return;
      end if;
      S.Nskinbone := Integer (V);
      V := Get_I64 (B, P + 408);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 51);
         return;
      end if;
      S.Nskinbonevert := Integer (V);
      V := Get_I64 (B, P + 416);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 52);
         return;
      end if;
      S.Nhfield := Integer (V);
      V := Get_I64 (B, P + 424);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 53);
         return;
      end if;
      S.Nhfielddata := Integer (V);
      V := Get_I64 (B, P + 432);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 54);
         return;
      end if;
      S.Ntex := Integer (V);
      V := Get_I64 (B, P + 440);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 55);
         return;
      end if;
      S.Ntexdata := Integer (V);
      V := Get_I64 (B, P + 448);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 56);
         return;
      end if;
      S.Nmat := Integer (V);
      V := Get_I64 (B, P + 456);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 57);
         return;
      end if;
      S.Npair := Integer (V);
      V := Get_I64 (B, P + 464);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 58);
         return;
      end if;
      S.Nexclude := Integer (V);
      V := Get_I64 (B, P + 472);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 59);
         return;
      end if;
      S.Neq := Integer (V);
      V := Get_I64 (B, P + 480);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 60);
         return;
      end if;
      S.Ntendon := Integer (V);
      V := Get_I64 (B, P + 488);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 61);
         return;
      end if;
      S.NJten := Integer (V);
      V := Get_I64 (B, P + 496);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 62);
         return;
      end if;
      S.Nwrap := Integer (V);
      V := Get_I64 (B, P + 504);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 63);
         return;
      end if;
      S.Nsensor := Integer (V);
      V := Get_I64 (B, P + 512);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 64);
         return;
      end if;
      S.Nnumeric := Integer (V);
      V := Get_I64 (B, P + 520);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 65);
         return;
      end if;
      S.Nnumericdata := Integer (V);
      V := Get_I64 (B, P + 528);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 66);
         return;
      end if;
      S.Ntext := Integer (V);
      V := Get_I64 (B, P + 536);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 67);
         return;
      end if;
      S.Ntextdata := Integer (V);
      V := Get_I64 (B, P + 544);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 68);
         return;
      end if;
      S.Ntuple := Integer (V);
      V := Get_I64 (B, P + 552);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 69);
         return;
      end if;
      S.Ntupledata := Integer (V);
      V := Get_I64 (B, P + 560);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 70);
         return;
      end if;
      S.Nkey := Integer (V);
      V := Get_I64 (B, P + 568);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 71);
         return;
      end if;
      S.Nmocap := Integer (V);
      V := Get_I64 (B, P + 576);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 72);
         return;
      end if;
      S.Nplugin := Integer (V);
      V := Get_I64 (B, P + 584);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 73);
         return;
      end if;
      S.Npluginattr := Integer (V);
      V := Get_I64 (B, P + 592);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 74);
         return;
      end if;
      S.Nuser_Body := Integer (V);
      V := Get_I64 (B, P + 600);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 75);
         return;
      end if;
      S.Nuser_Jnt := Integer (V);
      V := Get_I64 (B, P + 608);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 76);
         return;
      end if;
      S.Nuser_Geom := Integer (V);
      V := Get_I64 (B, P + 616);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 77);
         return;
      end if;
      S.Nuser_Site := Integer (V);
      V := Get_I64 (B, P + 624);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 78);
         return;
      end if;
      S.Nuser_Cam := Integer (V);
      V := Get_I64 (B, P + 632);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 79);
         return;
      end if;
      S.Nuser_Tendon := Integer (V);
      V := Get_I64 (B, P + 640);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 80);
         return;
      end if;
      S.Nuser_Actuator := Integer (V);
      V := Get_I64 (B, P + 648);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 81);
         return;
      end if;
      S.Nuser_Sensor := Integer (V);
      V := Get_I64 (B, P + 656);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 82);
         return;
      end if;
      S.Nnames := Integer (V);
      V := Get_I64 (B, P + 664);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 83);
         return;
      end if;
      S.Npaths := Integer (V);
      V := Get_I64 (B, P + 672);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 84);
         return;
      end if;
      S.Nnames_Map := Integer (V);
      V := Get_I64 (B, P + 680);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 85);
         return;
      end if;
      S.NJmom := Integer (V);
      V := Get_I64 (B, P + 688);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 86);
         return;
      end if;
      S.Ngravcomp := Integer (V);
      V := Get_I64 (B, P + 696);
      if V not in -1 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 87);
         return;
      end if;
      S.Nemax := Integer (V);
      V := Get_I64 (B, P + 704);
      if V not in -1 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 88);
         return;
      end if;
      S.Njmax := Integer (V);
      V := Get_I64 (B, P + 712);
      if V not in -1 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 89);
         return;
      end if;
      S.Nconmax := Integer (V);
      V := Get_I64 (B, P + 720);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 90);
         return;
      end if;
      S.Npolygonmax := Integer (V);
      V := Get_I64 (B, P + 728);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 91);
         return;
      end if;
      S.Nmeshdegmax := Integer (V);
      V := Get_I64 (B, P + 736);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 92);
         return;
      end if;
      S.Nuserdata := Integer (V);
      V := Get_I64 (B, P + 744);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 93);
         return;
      end if;
      S.Nsensordata := Integer (V);
      V := Get_I64 (B, P + 752);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 94);
         return;
      end if;
      S.Npluginstate := Integer (V);
      V := Get_I64 (B, P + 760);
      if V not in 0 .. Int64 (Max_Size) then
         Result := (Size_Out_Of_Range, Size_Table, 95);
         return;
      end if;
      S.Nhistory := Integer (V);
      V := Get_I64 (B, P + 768);
      S.Narena := V;
      V := Get_I64 (B, P + 776);
      S.Nbuffer := V;
      Pos := P + 8 * Size_Count;
   end Read_Sizes;

   procedure Read_Fixed (B : Byte_Array; Pos : in out Natural; Opt : out Option; Vis : out Visual;
                         Stat : out Statistic; Gravcomp, Surfacevel : out Boolean; Result : out Load_Result) is
      Base : Natural := Pos;
      U64  : Unsigned_64;
      U32  : Unsigned_32;
   begin
      Result := OK_Result;
      Opt := (others => <>);
      Vis := (others => <>);
      Stat := (others => <>);
      Gravcomp := False;
      Surfacevel := False;
      --  Option_Block
      U64 := Get_U64 (B, Base + Option_Off_Timestep);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 0); return; end if;
      Opt.Timestep := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Impratio);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 1); return; end if;
      Opt.Impratio := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Tolerance);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 2); return; end if;
      Opt.Tolerance := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Ls_Tolerance);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 3); return; end if;
      Opt.Ls_Tolerance := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Noslip_Tolerance);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 4); return; end if;
      Opt.Noslip_Tolerance := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Ccd_Tolerance);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 5); return; end if;
      Opt.Ccd_Tolerance := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Sleep_Tolerance);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 6); return; end if;
      Opt.Sleep_Tolerance := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Gravity + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 7); return; end if;
      Opt.Gravity (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Gravity + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 7); return; end if;
      Opt.Gravity (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Gravity + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 7); return; end if;
      Opt.Gravity (2) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Wind + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 8); return; end if;
      Opt.Wind (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Wind + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 8); return; end if;
      Opt.Wind (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Wind + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 8); return; end if;
      Opt.Wind (2) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Magnetic + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 9); return; end if;
      Opt.Magnetic (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Magnetic + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 9); return; end if;
      Opt.Magnetic (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Magnetic + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 9); return; end if;
      Opt.Magnetic (2) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Density);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 10); return; end if;
      Opt.Density := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_Viscosity);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 11); return; end if;
      Opt.Viscosity := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Margin);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 12); return; end if;
      Opt.O_Margin := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solref + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 13); return; end if;
      Opt.O_Solref (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solref + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 13); return; end if;
      Opt.O_Solref (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solimp + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 14); return; end if;
      Opt.O_Solimp (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solimp + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 14); return; end if;
      Opt.O_Solimp (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solimp + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 14); return; end if;
      Opt.O_Solimp (2) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solimp + 24);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 14); return; end if;
      Opt.O_Solimp (3) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Solimp + 32);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 14); return; end if;
      Opt.O_Solimp (4) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Friction + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 15); return; end if;
      Opt.O_Friction (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Friction + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 15); return; end if;
      Opt.O_Friction (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Friction + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 15); return; end if;
      Opt.O_Friction (2) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Friction + 24);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 15); return; end if;
      Opt.O_Friction (3) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Option_Off_O_Friction + 32);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Option_Block, 15); return; end if;
      Opt.O_Friction (4) := Bits_To_Real (U64);
      Opt.Integrator := Get_I32 (B, Base + Option_Off_Integrator);
      Opt.Cone := Get_I32 (B, Base + Option_Off_Cone);
      Opt.Jacobian := Get_I32 (B, Base + Option_Off_Jacobian);
      Opt.Solver := Get_I32 (B, Base + Option_Off_Solver);
      Opt.Iterations := Get_I32 (B, Base + Option_Off_Iterations);
      Opt.Ls_Iterations := Get_I32 (B, Base + Option_Off_Ls_Iterations);
      Opt.Noslip_Iterations := Get_I32 (B, Base + Option_Off_Noslip_Iterations);
      Opt.Ccd_Iterations := Get_I32 (B, Base + Option_Off_Ccd_Iterations);
      Opt.Disableflags := Get_I32 (B, Base + Option_Off_Disableflags);
      Opt.Enableflags := Get_I32 (B, Base + Option_Off_Enableflags);
      Opt.Disableactuator := Get_I32 (B, Base + Option_Off_Disableactuator);
      Opt.Sdf_Initpoints := Get_I32 (B, Base + Option_Off_Sdf_Initpoints);
      Opt.Sdf_Iterations := Get_I32 (B, Base + Option_Off_Sdf_Iterations);
      Base := Base + Option_Size;
      --  Visual_Block
      Vis.Global.Cameraid := Get_I32 (B, Base + Visual_Off_Global_Cameraid);
      Vis.Global.Orthographic := Get_I32 (B, Base + Visual_Off_Global_Orthographic);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Fovy);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 2); return; end if;
      Vis.Global.Fovy := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Ipd);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 3); return; end if;
      Vis.Global.Ipd := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Azimuth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 4); return; end if;
      Vis.Global.Azimuth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Elevation);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 5); return; end if;
      Vis.Global.Elevation := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Linewidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 6); return; end if;
      Vis.Global.Linewidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Glow);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 7); return; end if;
      Vis.Global.Glow := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Global_Realtime);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 8); return; end if;
      Vis.Global.Realtime := Bits_To_Float (U32);
      Vis.Global.Offwidth := Get_I32 (B, Base + Visual_Off_Global_Offwidth);
      Vis.Global.Offheight := Get_I32 (B, Base + Visual_Off_Global_Offheight);
      Vis.Global.Ellipsoidinertia := Get_I32 (B, Base + Visual_Off_Global_Ellipsoidinertia);
      Vis.Global.Bvactive := Get_I32 (B, Base + Visual_Off_Global_Bvactive);
      Vis.Quality.Shadowsize := Get_I32 (B, Base + Visual_Off_Quality_Shadowsize);
      Vis.Quality.Offsamples := Get_I32 (B, Base + Visual_Off_Quality_Offsamples);
      Vis.Quality.Numslices := Get_I32 (B, Base + Visual_Off_Quality_Numslices);
      Vis.Quality.Numstacks := Get_I32 (B, Base + Visual_Off_Quality_Numstacks);
      Vis.Quality.Numquads := Get_I32 (B, Base + Visual_Off_Quality_Numquads);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Ambient + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 18); return; end if;
      Vis.Headlight.Ambient (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Ambient + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 18); return; end if;
      Vis.Headlight.Ambient (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Ambient + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 18); return; end if;
      Vis.Headlight.Ambient (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Diffuse + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 19); return; end if;
      Vis.Headlight.Diffuse (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Diffuse + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 19); return; end if;
      Vis.Headlight.Diffuse (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Diffuse + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 19); return; end if;
      Vis.Headlight.Diffuse (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Specular + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 20); return; end if;
      Vis.Headlight.Specular (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Specular + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 20); return; end if;
      Vis.Headlight.Specular (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Headlight_Specular + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 20); return; end if;
      Vis.Headlight.Specular (2) := Bits_To_Float (U32);
      Vis.Headlight.Active := Get_I32 (B, Base + Visual_Off_Headlight_Active);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Stiffness);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 22); return; end if;
      Vis.Map.Stiffness := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Stiffnessrot);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 23); return; end if;
      Vis.Map.Stiffnessrot := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Force);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 24); return; end if;
      Vis.Map.Force := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Torque);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 25); return; end if;
      Vis.Map.Torque := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Alpha);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 26); return; end if;
      Vis.Map.Alpha := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Fogstart);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 27); return; end if;
      Vis.Map.Fogstart := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Fogend);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 28); return; end if;
      Vis.Map.Fogend := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Znear);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 29); return; end if;
      Vis.Map.Znear := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Zfar);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 30); return; end if;
      Vis.Map.Zfar := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Haze);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 31); return; end if;
      Vis.Map.Haze := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Shadowclip);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 32); return; end if;
      Vis.Map.Shadowclip := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Shadowscale);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 33); return; end if;
      Vis.Map.Shadowscale := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Map_Actuatortendon);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 34); return; end if;
      Vis.Map.Actuatortendon := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Forcewidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 35); return; end if;
      Vis.Scale.Forcewidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Contactwidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 36); return; end if;
      Vis.Scale.Contactwidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Contactheight);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 37); return; end if;
      Vis.Scale.Contactheight := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Connect);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 38); return; end if;
      Vis.Scale.Connect := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Com);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 39); return; end if;
      Vis.Scale.Com := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Camera);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 40); return; end if;
      Vis.Scale.Camera := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Light);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 41); return; end if;
      Vis.Scale.Light := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Selectpoint);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 42); return; end if;
      Vis.Scale.Selectpoint := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Jointlength);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 43); return; end if;
      Vis.Scale.Jointlength := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Jointwidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 44); return; end if;
      Vis.Scale.Jointwidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Actuatorlength);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 45); return; end if;
      Vis.Scale.Actuatorlength := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Actuatorwidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 46); return; end if;
      Vis.Scale.Actuatorwidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Framelength);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 47); return; end if;
      Vis.Scale.Framelength := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Framewidth);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 48); return; end if;
      Vis.Scale.Framewidth := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Constraint);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 49); return; end if;
      Vis.Scale.Constraint := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Slidercrank);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 50); return; end if;
      Vis.Scale.Slidercrank := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Scale_Frustum);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 51); return; end if;
      Vis.Scale.Frustum := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Fog + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 52); return; end if;
      Vis.Rgba.Fog (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Fog + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 52); return; end if;
      Vis.Rgba.Fog (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Fog + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 52); return; end if;
      Vis.Rgba.Fog (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Fog + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 52); return; end if;
      Vis.Rgba.Fog (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Haze + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 53); return; end if;
      Vis.Rgba.Haze (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Haze + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 53); return; end if;
      Vis.Rgba.Haze (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Haze + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 53); return; end if;
      Vis.Rgba.Haze (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Haze + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 53); return; end if;
      Vis.Rgba.Haze (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Force + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 54); return; end if;
      Vis.Rgba.Force (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Force + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 54); return; end if;
      Vis.Rgba.Force (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Force + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 54); return; end if;
      Vis.Rgba.Force (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Force + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 54); return; end if;
      Vis.Rgba.Force (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Inertia + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 55); return; end if;
      Vis.Rgba.Inertia (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Inertia + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 55); return; end if;
      Vis.Rgba.Inertia (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Inertia + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 55); return; end if;
      Vis.Rgba.Inertia (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Inertia + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 55); return; end if;
      Vis.Rgba.Inertia (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Joint + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 56); return; end if;
      Vis.Rgba.Joint (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Joint + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 56); return; end if;
      Vis.Rgba.Joint (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Joint + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 56); return; end if;
      Vis.Rgba.Joint (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Joint + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 56); return; end if;
      Vis.Rgba.Joint (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuator + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 57); return; end if;
      Vis.Rgba.Actuator (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuator + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 57); return; end if;
      Vis.Rgba.Actuator (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuator + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 57); return; end if;
      Vis.Rgba.Actuator (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuator + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 57); return; end if;
      Vis.Rgba.Actuator (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatornegative + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 58); return; end if;
      Vis.Rgba.Actuatornegative (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatornegative + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 58); return; end if;
      Vis.Rgba.Actuatornegative (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatornegative + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 58); return; end if;
      Vis.Rgba.Actuatornegative (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatornegative + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 58); return; end if;
      Vis.Rgba.Actuatornegative (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatorpositive + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 59); return; end if;
      Vis.Rgba.Actuatorpositive (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatorpositive + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 59); return; end if;
      Vis.Rgba.Actuatorpositive (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatorpositive + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 59); return; end if;
      Vis.Rgba.Actuatorpositive (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Actuatorpositive + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 59); return; end if;
      Vis.Rgba.Actuatorpositive (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Com + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 60); return; end if;
      Vis.Rgba.Com (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Com + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 60); return; end if;
      Vis.Rgba.Com (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Com + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 60); return; end if;
      Vis.Rgba.Com (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Com + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 60); return; end if;
      Vis.Rgba.Com (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Camera + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 61); return; end if;
      Vis.Rgba.Camera (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Camera + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 61); return; end if;
      Vis.Rgba.Camera (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Camera + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 61); return; end if;
      Vis.Rgba.Camera (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Camera + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 61); return; end if;
      Vis.Rgba.Camera (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Light + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 62); return; end if;
      Vis.Rgba.Light (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Light + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 62); return; end if;
      Vis.Rgba.Light (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Light + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 62); return; end if;
      Vis.Rgba.Light (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Light + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 62); return; end if;
      Vis.Rgba.Light (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Selectpoint + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 63); return; end if;
      Vis.Rgba.Selectpoint (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Selectpoint + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 63); return; end if;
      Vis.Rgba.Selectpoint (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Selectpoint + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 63); return; end if;
      Vis.Rgba.Selectpoint (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Selectpoint + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 63); return; end if;
      Vis.Rgba.Selectpoint (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Connect + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 64); return; end if;
      Vis.Rgba.Connect (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Connect + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 64); return; end if;
      Vis.Rgba.Connect (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Connect + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 64); return; end if;
      Vis.Rgba.Connect (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Connect + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 64); return; end if;
      Vis.Rgba.Connect (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactpoint + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 65); return; end if;
      Vis.Rgba.Contactpoint (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactpoint + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 65); return; end if;
      Vis.Rgba.Contactpoint (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactpoint + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 65); return; end if;
      Vis.Rgba.Contactpoint (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactpoint + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 65); return; end if;
      Vis.Rgba.Contactpoint (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactforce + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 66); return; end if;
      Vis.Rgba.Contactforce (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactforce + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 66); return; end if;
      Vis.Rgba.Contactforce (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactforce + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 66); return; end if;
      Vis.Rgba.Contactforce (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactforce + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 66); return; end if;
      Vis.Rgba.Contactforce (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactfriction + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 67); return; end if;
      Vis.Rgba.Contactfriction (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactfriction + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 67); return; end if;
      Vis.Rgba.Contactfriction (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactfriction + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 67); return; end if;
      Vis.Rgba.Contactfriction (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactfriction + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 67); return; end if;
      Vis.Rgba.Contactfriction (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contacttorque + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 68); return; end if;
      Vis.Rgba.Contacttorque (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contacttorque + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 68); return; end if;
      Vis.Rgba.Contacttorque (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contacttorque + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 68); return; end if;
      Vis.Rgba.Contacttorque (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contacttorque + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 68); return; end if;
      Vis.Rgba.Contacttorque (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactgap + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 69); return; end if;
      Vis.Rgba.Contactgap (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactgap + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 69); return; end if;
      Vis.Rgba.Contactgap (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactgap + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 69); return; end if;
      Vis.Rgba.Contactgap (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Contactgap + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 69); return; end if;
      Vis.Rgba.Contactgap (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Rangefinder + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 70); return; end if;
      Vis.Rgba.Rangefinder (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Rangefinder + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 70); return; end if;
      Vis.Rgba.Rangefinder (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Rangefinder + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 70); return; end if;
      Vis.Rgba.Rangefinder (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Rangefinder + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 70); return; end if;
      Vis.Rgba.Rangefinder (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Constraint + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 71); return; end if;
      Vis.Rgba.Constraint (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Constraint + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 71); return; end if;
      Vis.Rgba.Constraint (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Constraint + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 71); return; end if;
      Vis.Rgba.Constraint (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Constraint + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 71); return; end if;
      Vis.Rgba.Constraint (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Slidercrank + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 72); return; end if;
      Vis.Rgba.Slidercrank (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Slidercrank + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 72); return; end if;
      Vis.Rgba.Slidercrank (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Slidercrank + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 72); return; end if;
      Vis.Rgba.Slidercrank (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Slidercrank + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 72); return; end if;
      Vis.Rgba.Slidercrank (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Crankbroken + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 73); return; end if;
      Vis.Rgba.Crankbroken (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Crankbroken + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 73); return; end if;
      Vis.Rgba.Crankbroken (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Crankbroken + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 73); return; end if;
      Vis.Rgba.Crankbroken (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Crankbroken + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 73); return; end if;
      Vis.Rgba.Crankbroken (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Frustum + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 74); return; end if;
      Vis.Rgba.Frustum (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Frustum + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 74); return; end if;
      Vis.Rgba.Frustum (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Frustum + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 74); return; end if;
      Vis.Rgba.Frustum (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Frustum + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 74); return; end if;
      Vis.Rgba.Frustum (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bv + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 75); return; end if;
      Vis.Rgba.Bv (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bv + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 75); return; end if;
      Vis.Rgba.Bv (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bv + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 75); return; end if;
      Vis.Rgba.Bv (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bv + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 75); return; end if;
      Vis.Rgba.Bv (3) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bvactive + 0);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 76); return; end if;
      Vis.Rgba.Bvactive (0) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bvactive + 4);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 76); return; end if;
      Vis.Rgba.Bvactive (1) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bvactive + 8);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 76); return; end if;
      Vis.Rgba.Bvactive (2) := Bits_To_Float (U32);
      U32 := Get_U32 (B, Base + Visual_Off_Rgba_Bvactive + 12);
      if not Is_Finite_F32 (U32) then Result := (Non_Finite_Value, Visual_Block, 76); return; end if;
      Vis.Rgba.Bvactive (3) := Bits_To_Float (U32);
      Base := Base + Visual_Size;
      --  Statistic_Block
      U64 := Get_U64 (B, Base + Statistic_Off_Meaninertia);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 0); return; end if;
      Stat.Meaninertia := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Meanmass);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 1); return; end if;
      Stat.Meanmass := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Meansize);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 2); return; end if;
      Stat.Meansize := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Extent);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 3); return; end if;
      Stat.Extent := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Center + 0);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 4); return; end if;
      Stat.Center (0) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Center + 8);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 4); return; end if;
      Stat.Center (1) := Bits_To_Real (U64);
      U64 := Get_U64 (B, Base + Statistic_Off_Center + 16);
      if not Is_Finite_F64 (U64) then Result := (Non_Finite_Value, Statistic_Block, 4); return; end if;
      Stat.Center (2) := Bits_To_Real (U64);
      Base := Base + Statistic_Size;
      Gravcomp := Get_U8 (B, Base) /= 0;
      Surfacevel := Get_U8 (B, Base + Bool_Size) /= 0;
      Pos := Base + 2 * Bool_Size;
   end Read_Fixed;

   procedure Read_Body (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Body_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  body_parentid : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Parentid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Parentid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_rootid : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Rootid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Rootid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_weldid : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Weldid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Weldid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_mocapid : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Mocapid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Mocapid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_jntnum : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Jntnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Jntnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_jntadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Jntadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Jntadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_dofnum : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Dofnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Dofnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_dofadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Dofadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Dofadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_treeid : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Treeid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Treeid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_geomnum : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Geomnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Geomnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_geomadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Geomadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Geomadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_simple : mjtByte (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Simple, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Simple (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_sameframe : mjtByte (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Sameframe, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Sameframe (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_pos : mjtNum (nbody x 3)
      Count  := Int64 (S.Nbody) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Pos, I);
               return;
            end if;
            G.Body_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_quat : mjtNum (nbody x 4)
      Count  := Int64 (S.Nbody) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Quat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Quat, I);
               return;
            end if;
            G.Body_Quat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_ipos : mjtNum (nbody x 3)
      Count  := Int64 (S.Nbody) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Ipos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Ipos, I);
               return;
            end if;
            G.Body_Ipos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_iquat : mjtNum (nbody x 4)
      Count  := Int64 (S.Nbody) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Iquat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Iquat, I);
               return;
            end if;
            G.Body_Iquat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_mass : mjtNum (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Mass, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Mass, I);
               return;
            end if;
            G.Body_Mass (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_subtreemass : mjtNum (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Subtreemass, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Subtreemass, I);
               return;
            end if;
            G.Body_Subtreemass (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_inertia : mjtNum (nbody x 3)
      Count  := Int64 (S.Nbody) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Inertia, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Inertia, I);
               return;
            end if;
            G.Body_Inertia (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_invweight0 : mjtNum (nbody x 2)
      Count  := Int64 (S.Nbody) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Invweight0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Invweight0, I);
               return;
            end if;
            G.Body_Invweight0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_gravcomp : mjtNum (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Gravcomp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Gravcomp, I);
               return;
            end if;
            G.Body_Gravcomp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_margin : mjtNum (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_Margin, I);
               return;
            end if;
            G.Body_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_user : mjtNum (nbody x MJ_M(nuser_body))
      Count  := Int64 (S.Nbody) * (Int64 (S.Nuser_Body));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Body_User, I);
               return;
            end if;
            G.Body_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_plugin : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Plugin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Plugin (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_contype : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Contype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Contype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_conaffinity : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Conaffinity, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Conaffinity (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_bvhadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Bvhadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Bvhadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  body_bvhnum : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Body_Bvhnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Body_Layout_OK (S, G));
         G.Body_Bvhnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Body;

   procedure Read_Joint (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Joint_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  jnt_type : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_qposadr : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Qposadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Qposadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_dofadr : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Dofadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Dofadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_bodyid : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_actuatorid : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Actuatorid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Actuatorid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_group : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_limited : mjtBool (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Limited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Limited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_actfrclimited : mjtBool (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Actfrclimited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Actfrclimited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_actgravcomp : mjtBool (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Actgravcomp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         G.Jnt_Actgravcomp (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_solref : mjtNum (njnt x mjNREF)
      Count  := Int64 (S.Njnt) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Solref, I);
               return;
            end if;
            G.Jnt_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_solimp : mjtNum (njnt x mjNIMP)
      Count  := Int64 (S.Njnt) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Solimp, I);
               return;
            end if;
            G.Jnt_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_pos : mjtNum (njnt x 3)
      Count  := Int64 (S.Njnt) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Pos, I);
               return;
            end if;
            G.Jnt_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_axis : mjtNum (njnt x 3)
      Count  := Int64 (S.Njnt) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Axis, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Axis, I);
               return;
            end if;
            G.Jnt_Axis (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_stiffness : mjtNum (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Stiffness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Stiffness, I);
               return;
            end if;
            G.Jnt_Stiffness (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_stiffnesspoly : mjtNum (njnt x mjNPOLY)
      Count  := Int64 (S.Njnt) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Stiffnesspoly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Stiffnesspoly, I);
               return;
            end if;
            G.Jnt_Stiffnesspoly (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_range : mjtNum (njnt x 2)
      Count  := Int64 (S.Njnt) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Range, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Range, I);
               return;
            end if;
            G.Jnt_Range (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_actfrcrange : mjtNum (njnt x 2)
      Count  := Int64 (S.Njnt) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Actfrcrange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Actfrcrange, I);
               return;
            end if;
            G.Jnt_Actfrcrange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_margin : mjtNum (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_Margin, I);
               return;
            end if;
            G.Jnt_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  jnt_user : mjtNum (njnt x MJ_M(nuser_jnt))
      Count  := Int64 (S.Njnt) * (Int64 (S.Nuser_Jnt));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Jnt_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Joint_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Jnt_User, I);
               return;
            end if;
            G.Jnt_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Joint;

   procedure Read_Dof (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Dof_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  dof_bodyid : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_jntid : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Jntid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Jntid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_parentid : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Parentid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Parentid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_treeid : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Treeid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Treeid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_Madr : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Madr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Madr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_simplenum : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Simplenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         G.Dof_Simplenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_solref : mjtNum (nv x mjNREF)
      Count  := Int64 (S.Nv) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Solref, I);
               return;
            end if;
            G.Dof_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_solimp : mjtNum (nv x mjNIMP)
      Count  := Int64 (S.Nv) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Solimp, I);
               return;
            end if;
            G.Dof_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_frictionloss : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Frictionloss, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Frictionloss, I);
               return;
            end if;
            G.Dof_Frictionloss (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_armature : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Armature, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Armature, I);
               return;
            end if;
            G.Dof_Armature (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_damping : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Damping, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Damping, I);
               return;
            end if;
            G.Dof_Damping (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_dampingpoly : mjtNum (nv x mjNPOLY)
      Count  := Int64 (S.Nv) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Dampingpoly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Dampingpoly, I);
               return;
            end if;
            G.Dof_Dampingpoly (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_invweight0 : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Invweight0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Invweight0, I);
               return;
            end if;
            G.Dof_Invweight0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_M0 : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_M0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_M0, I);
               return;
            end if;
            G.Dof_M0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  dof_length : mjtNum (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Dof_Length, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Dof_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Dof_Length, I);
               return;
            end if;
            G.Dof_Length (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Dof;

   procedure Read_Tree (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Tree_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  tree_bodyadr : int (ntree x 1)
      Count  := Int64 (S.Ntree) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tree_Bodyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tree_Layout_OK (S, G));
         G.Tree_Bodyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tree_bodynum : int (ntree x 1)
      Count  := Int64 (S.Ntree) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tree_Bodynum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tree_Layout_OK (S, G));
         G.Tree_Bodynum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tree_dofadr : int (ntree x 1)
      Count  := Int64 (S.Ntree) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tree_Dofadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tree_Layout_OK (S, G));
         G.Tree_Dofadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tree_dofnum : int (ntree x 1)
      Count  := Int64 (S.Ntree) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tree_Dofnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tree_Layout_OK (S, G));
         G.Tree_Dofnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tree_sleep_policy : int (ntree x 1)
      Count  := Int64 (S.Ntree) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tree_Sleep_Policy, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tree_Layout_OK (S, G));
         G.Tree_Sleep_Policy (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Tree;

   procedure Read_Geom (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Geom_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  geom_type : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_contype : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Contype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Contype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_conaffinity : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Conaffinity, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Conaffinity (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_condim : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Condim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Condim (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_bodyid : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_dataid : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Dataid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Dataid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_matid : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Matid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Matid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_group : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_priority : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Priority, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Priority (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_plugin : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Plugin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Plugin (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_sameframe : mjtByte (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Sameframe, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         G.Geom_Sameframe (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_solmix : mjtNum (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Solmix, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Solmix, I);
               return;
            end if;
            G.Geom_Solmix (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_solref : mjtNum (ngeom x mjNREF)
      Count  := Int64 (S.Ngeom) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Solref, I);
               return;
            end if;
            G.Geom_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_solimp : mjtNum (ngeom x mjNIMP)
      Count  := Int64 (S.Ngeom) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Solimp, I);
               return;
            end if;
            G.Geom_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_size : mjtNum (ngeom x 3)
      Count  := Int64 (S.Ngeom) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Size, I);
               return;
            end if;
            G.Geom_Size (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_aabb : mjtNum (ngeom x 6)
      Count  := Int64 (S.Ngeom) * (6);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Aabb, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Aabb, I);
               return;
            end if;
            G.Geom_Aabb (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_rbound : mjtNum (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Rbound, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Rbound, I);
               return;
            end if;
            G.Geom_Rbound (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_pos : mjtNum (ngeom x 3)
      Count  := Int64 (S.Ngeom) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Pos, I);
               return;
            end if;
            G.Geom_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_quat : mjtNum (ngeom x 4)
      Count  := Int64 (S.Ngeom) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Quat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Quat, I);
               return;
            end if;
            G.Geom_Quat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_friction : mjtNum (ngeom x 3)
      Count  := Int64 (S.Ngeom) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Friction, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Friction, I);
               return;
            end if;
            G.Geom_Friction (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_margin : mjtNum (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Margin, I);
               return;
            end if;
            G.Geom_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_gap : mjtNum (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Gap, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Gap, I);
               return;
            end if;
            G.Geom_Gap (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_surfacevel : mjtNum (ngeom x 6)
      Count  := Int64 (S.Ngeom) * (6);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Surfacevel, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Surfacevel, I);
               return;
            end if;
            G.Geom_Surfacevel (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_adhesion : mjtNum (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Adhesion, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Adhesion, I);
               return;
            end if;
            G.Geom_Adhesion (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_fluid : mjtNum (ngeom x mjNFLUID)
      Count  := Int64 (S.Ngeom) * (12);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Fluid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_Fluid, I);
               return;
            end if;
            G.Geom_Fluid (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_user : mjtNum (ngeom x MJ_M(nuser_geom))
      Count  := Int64 (S.Ngeom) * (Int64 (S.Nuser_Geom));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Geom_User, I);
               return;
            end if;
            G.Geom_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  geom_rgba : float (ngeom x 4)
      Count  := Int64 (S.Ngeom) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Geom_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Geom_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Geom_Rgba, I);
               return;
            end if;
            G.Geom_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Geom;

   procedure Read_Site (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Site_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  site_type : int (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         G.Site_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_bodyid : int (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         G.Site_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_matid : int (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Matid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         G.Site_Matid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_group : int (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         G.Site_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_sameframe : mjtByte (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Sameframe, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         G.Site_Sameframe (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_size : mjtNum (nsite x 3)
      Count  := Int64 (S.Nsite) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Site_Size, I);
               return;
            end if;
            G.Site_Size (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_pos : mjtNum (nsite x 3)
      Count  := Int64 (S.Nsite) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Site_Pos, I);
               return;
            end if;
            G.Site_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_quat : mjtNum (nsite x 4)
      Count  := Int64 (S.Nsite) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Quat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Site_Quat, I);
               return;
            end if;
            G.Site_Quat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_user : mjtNum (nsite x MJ_M(nuser_site))
      Count  := Int64 (S.Nsite) * (Int64 (S.Nuser_Site));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Site_User, I);
               return;
            end if;
            G.Site_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  site_rgba : float (nsite x 4)
      Count  := Int64 (S.Nsite) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Site_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Site_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Site_Rgba, I);
               return;
            end if;
            G.Site_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Site;

   procedure Read_Camera (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Camera_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  cam_mode : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Mode, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Mode (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_bodyid : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_targetbodyid : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Targetbodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Targetbodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_pos : mjtNum (ncam x 3)
      Count  := Int64 (S.Ncam) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Pos, I);
               return;
            end if;
            G.Cam_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_quat : mjtNum (ncam x 4)
      Count  := Int64 (S.Ncam) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Quat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Quat, I);
               return;
            end if;
            G.Cam_Quat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_poscom0 : mjtNum (ncam x 3)
      Count  := Int64 (S.Ncam) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Poscom0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Poscom0, I);
               return;
            end if;
            G.Cam_Poscom0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_pos0 : mjtNum (ncam x 3)
      Count  := Int64 (S.Ncam) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Pos0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Pos0, I);
               return;
            end if;
            G.Cam_Pos0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_mat0 : mjtNum (ncam x 9)
      Count  := Int64 (S.Ncam) * (9);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Mat0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Mat0, I);
               return;
            end if;
            G.Cam_Mat0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_projection : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Projection, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Projection (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_fovy : mjtNum (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Fovy, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Fovy, I);
               return;
            end if;
            G.Cam_Fovy (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_ipd : mjtNum (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Ipd, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_Ipd, I);
               return;
            end if;
            G.Cam_Ipd (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_resolution : int (ncam x 2)
      Count  := Int64 (S.Ncam) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Resolution, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Resolution (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_output : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Output, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         G.Cam_Output (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_sensorsize : float (ncam x 2)
      Count  := Int64 (S.Ncam) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Sensorsize, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Cam_Sensorsize, I);
               return;
            end if;
            G.Cam_Sensorsize (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_intrinsic : float (ncam x 4)
      Count  := Int64 (S.Ncam) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_Intrinsic, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Cam_Intrinsic, I);
               return;
            end if;
            G.Cam_Intrinsic (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  cam_user : mjtNum (ncam x MJ_M(nuser_cam))
      Count  := Int64 (S.Ncam) * (Int64 (S.Nuser_Cam));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Cam_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Camera_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Cam_User, I);
               return;
            end if;
            G.Cam_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Camera;

   procedure Read_Light (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Light_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  light_mode : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Mode, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Mode (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_bodyid : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Bodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Bodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_targetbodyid : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Targetbodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Targetbodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_type : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_texid : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Texid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Texid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_castshadow : mjtBool (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Castshadow, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Castshadow (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_bulbradius : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Bulbradius, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Bulbradius, I);
               return;
            end if;
            G.Light_Bulbradius (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_intensity : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Intensity, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Intensity, I);
               return;
            end if;
            G.Light_Intensity (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_range : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Range, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Range, I);
               return;
            end if;
            G.Light_Range (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_active : mjtBool (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Active, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         G.Light_Active (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_pos : mjtNum (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Light_Pos, I);
               return;
            end if;
            G.Light_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_dir : mjtNum (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Dir, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Light_Dir, I);
               return;
            end if;
            G.Light_Dir (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_poscom0 : mjtNum (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Poscom0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Light_Poscom0, I);
               return;
            end if;
            G.Light_Poscom0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_pos0 : mjtNum (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Pos0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Light_Pos0, I);
               return;
            end if;
            G.Light_Pos0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_dir0 : mjtNum (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Dir0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Light_Dir0, I);
               return;
            end if;
            G.Light_Dir0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_attenuation : float (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Attenuation, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Attenuation, I);
               return;
            end if;
            G.Light_Attenuation (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_cutoff : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Cutoff, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Cutoff, I);
               return;
            end if;
            G.Light_Cutoff (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_softness : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Softness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Softness, I);
               return;
            end if;
            G.Light_Softness (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_exponent : float (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Exponent, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Exponent, I);
               return;
            end if;
            G.Light_Exponent (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_ambient : float (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Ambient, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Ambient, I);
               return;
            end if;
            G.Light_Ambient (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_diffuse : float (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Diffuse, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Diffuse, I);
               return;
            end if;
            G.Light_Diffuse (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  light_specular : float (nlight x 3)
      Count  := Int64 (S.Nlight) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Light_Specular, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Light_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Light_Specular, I);
               return;
            end if;
            G.Light_Specular (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Light;

   procedure Read_Flex (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Flex_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  flex_contype : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Contype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Contype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_conaffinity : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Conaffinity, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Conaffinity (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_condim : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Condim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Condim (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_priority : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Priority, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Priority (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_solmix : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Solmix, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Solmix, I);
               return;
            end if;
            G.Flex_Solmix (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_solref : mjtNum (nflex x mjNREF)
      Count  := Int64 (S.Nflex) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Solref, I);
               return;
            end if;
            G.Flex_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_solimp : mjtNum (nflex x mjNIMP)
      Count  := Int64 (S.Nflex) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Solimp, I);
               return;
            end if;
            G.Flex_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_friction : mjtNum (nflex x 3)
      Count  := Int64 (S.Nflex) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Friction, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Friction, I);
               return;
            end if;
            G.Flex_Friction (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_margin : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Margin, I);
               return;
            end if;
            G.Flex_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_gap : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Gap, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Gap, I);
               return;
            end if;
            G.Flex_Gap (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_internal : mjtBool (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Internal, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Internal (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_selfcollide : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Selfcollide, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Selfcollide (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_activelayers : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Activelayers, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Activelayers (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_passive : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Passive, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Passive (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_dim : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Dim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Dim (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_matid : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Matid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Matid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_group : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_interp : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Interp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Interp (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_cellnum : int (nflex x 3)
      Count  := Int64 (S.Nflex) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Cellnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Cellnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_nodeadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Nodeadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Nodeadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_nodenum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Nodenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Nodenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertnum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgeadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgeadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Edgeadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgenum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Edgenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemnum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemdataadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemdataadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemdataadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_stiffnessadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Stiffnessadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Stiffnessadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemedgeadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemedgeadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemedgeadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_bendingadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Bendingadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Bendingadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_shellnum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Shellnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Shellnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_shelldataadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Shelldataadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Shelldataadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_evpairadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Evpairadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Evpairadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_evpairnum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Evpairnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Evpairnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_texcoordadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Texcoordadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Texcoordadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_nodebodyid : int (nflexnode x 1)
      Count  := Int64 (S.Nflexnode) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Nodebodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Nodebodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertbodyid : int (nflexvert x 1)
      Count  := Int64 (S.Nflexvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertbodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertbodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertedgeadr : int (nflexvert x 1)
      Count  := Int64 (S.Nflexvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertedgeadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertedgeadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertedgenum : int (nflexvert x 1)
      Count  := Int64 (S.Nflexvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertedgenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertedgenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertedge : int (nflexedge x 2)
      Count  := Int64 (S.Nflexedge) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertedge, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Vertedge (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edge : int (nflexedge x 2)
      Count  := Int64 (S.Nflexedge) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edge, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Edge (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgeflap : int (nflexedge x 2)
      Count  := Int64 (S.Nflexedge) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgeflap, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Edgeflap (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elem : int (nflexelemdata x 1)
      Count  := Int64 (S.Nflexelemdata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elem, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elem (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemtexcoord : int (nflexelemdata x 1)
      Count  := Int64 (S.Nflexelemdata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemtexcoord, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemtexcoord (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemedge : int (nflexelemedge x 1)
      Count  := Int64 (S.Nflexelemedge) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemedge, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemedge (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_elemlayer : int (nflexelem x 1)
      Count  := Int64 (S.Nflexelem) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Elemlayer, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Elemlayer (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_shell : int (nflexshelldata x 1)
      Count  := Int64 (S.Nflexshelldata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Shell, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Shell (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_evpair : int (nflexevpair x 2)
      Count  := Int64 (S.Nflexevpair) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Evpair, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Evpair (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vert : mjtNum (nflexvert x 3)
      Count  := Int64 (S.Nflexvert) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vert, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Vert, I);
               return;
            end if;
            G.Flex_Vert (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vert0 : mjtNum (nflexvert x 3)
      Count  := Int64 (S.Nflexvert) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vert0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Vert0, I);
               return;
            end if;
            G.Flex_Vert0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_vertmetric : mjtNum (nflexvert x 4)
      Count  := Int64 (S.Nflexvert) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Vertmetric, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Vertmetric, I);
               return;
            end if;
            G.Flex_Vertmetric (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_node : mjtNum (nflexnode x 3)
      Count  := Int64 (S.Nflexnode) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Node, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Node, I);
               return;
            end if;
            G.Flex_Node (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_node0 : mjtNum (nflexnode x 3)
      Count  := Int64 (S.Nflexnode) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Node0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Node0, I);
               return;
            end if;
            G.Flex_Node0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_length0 : mjtNum (nflexedge x 1)
      Count  := Int64 (S.Nflexedge) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_Length0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flexedge_Length0, I);
               return;
            end if;
            G.Flexedge_Length0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_invweight0 : mjtNum (nflexedge x 1)
      Count  := Int64 (S.Nflexedge) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_Invweight0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flexedge_Invweight0, I);
               return;
            end if;
            G.Flexedge_Invweight0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_radius : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Radius, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Radius, I);
               return;
            end if;
            G.Flex_Radius (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_size : mjtNum (nflex x 3)
      Count  := Int64 (S.Nflex) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Size, I);
               return;
            end if;
            G.Flex_Size (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_stiffness : mjtNum (nflexstiffness x 1)
      Count  := Int64 (S.Nflexstiffness) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Stiffness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Stiffness, I);
               return;
            end if;
            G.Flex_Stiffness (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_bending : mjtNum (nflexbending x 1)
      Count  := Int64 (S.Nflexbending) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Bending, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Bending, I);
               return;
            end if;
            G.Flex_Bending (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  efm0_dofid : int (nefm0dof x 1)
      Count  := Int64 (S.Nefm0dof) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Efm0_Dofid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Efm0_Dofid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  efm0_L_rownnz : int (nefm0dof x 1)
      Count  := Int64 (S.Nefm0dof) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Efm0_L_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Efm0_L_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  efm0_L_rowadr : int (nefm0dof x 1)
      Count  := Int64 (S.Nefm0dof) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Efm0_L_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Efm0_L_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  efm0_L_colind : int (nefm0L x 1)
      Count  := Int64 (S.Nefm0L) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Efm0_L_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Efm0_L_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  efm0_L : mjtNum (nefm0L x 1)
      Count  := Int64 (S.Nefm0L) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Efm0_L, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Efm0_L, I);
               return;
            end if;
            G.Efm0_L (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_damping : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Damping, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Damping, I);
               return;
            end if;
            G.Flex_Damping (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgestiffness : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgestiffness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Edgestiffness, I);
               return;
            end if;
            G.Flex_Edgestiffness (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgedamping : mjtNum (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgedamping, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Flex_Edgedamping, I);
               return;
            end if;
            G.Flex_Edgedamping (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_edgeequality : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Edgeequality, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Edgeequality (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_rigid : mjtBool (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Rigid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Rigid (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_rigid : mjtBool (nflexedge x 1)
      Count  := Int64 (S.Nflexedge) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_Rigid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexedge_Rigid (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_centered : mjtBool (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Centered, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Centered (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_flatskin : mjtBool (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Flatskin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Flatskin (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_bvhadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Bvhadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Bvhadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_bvhnum : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Bvhnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flex_Bvhnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_J_rownnz : int (nflexedge x 1)
      Count  := Int64 (S.Nflexedge) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_J_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexedge_J_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_J_rowadr : int (nflexedge x 1)
      Count  := Int64 (S.Nflexedge) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_J_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexedge_J_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexedge_J_colind : int (nJfe x 1)
      Count  := Int64 (S.NJfe) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexedge_J_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexedge_J_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexvert_J_rownnz : int (nflexvert x 2)
      Count  := Int64 (S.Nflexvert) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexvert_J_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexvert_J_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexvert_J_rowadr : int (nflexvert x 2)
      Count  := Int64 (S.Nflexvert) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexvert_J_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexvert_J_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flexvert_J_colind : int (nJfv x 2)
      Count  := Int64 (S.NJfv) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flexvert_J_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         G.Flexvert_J_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_rgba : float (nflex x 4)
      Count  := Int64 (S.Nflex) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Flex_Rgba, I);
               return;
            end if;
            G.Flex_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  flex_texcoord : float (nflextexcoord x 2)
      Count  := Int64 (S.Nflextexcoord) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Flex_Texcoord, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Flex_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Flex_Texcoord, I);
               return;
            end if;
            G.Flex_Texcoord (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Flex;

   procedure Read_Mesh (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Mesh_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  mesh_vertadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Vertadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Vertadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_vertnum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Vertnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Vertnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_faceadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Faceadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Faceadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_facenum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Facenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Facenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_bvhadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Bvhadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Bvhadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_bvhnum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Bvhnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Bvhnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_octadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Octadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Octadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_octnum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Octnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Octnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_normaladr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Normaladr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Normaladr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_normalnum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Normalnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Normalnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_texcoordadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Texcoordadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Texcoordadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_texcoordnum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Texcoordnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Texcoordnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_graphadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Graphadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Graphadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_extrema : int (nmesh x 27)
      Count  := Int64 (S.Nmesh) * (27);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Extrema, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Extrema (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_vert : float (nmeshvert x 3)
      Count  := Int64 (S.Nmeshvert) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Vert, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mesh_Vert, I);
               return;
            end if;
            G.Mesh_Vert (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_normal : float (nmeshnormal x 3)
      Count  := Int64 (S.Nmeshnormal) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Normal, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mesh_Normal, I);
               return;
            end if;
            G.Mesh_Normal (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_texcoord : float (nmeshtexcoord x 2)
      Count  := Int64 (S.Nmeshtexcoord) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Texcoord, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mesh_Texcoord, I);
               return;
            end if;
            G.Mesh_Texcoord (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_face : int (nmeshface x 3)
      Count  := Int64 (S.Nmeshface) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Face, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Face (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_facenormal : int (nmeshface x 3)
      Count  := Int64 (S.Nmeshface) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Facenormal, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Facenormal (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_facetexcoord : int (nmeshface x 3)
      Count  := Int64 (S.Nmeshface) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Facetexcoord, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Facetexcoord (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_graph : int (nmeshgraph x 1)
      Count  := Int64 (S.Nmeshgraph) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Graph, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Graph (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_scale : mjtNum (nmesh x 3)
      Count  := Int64 (S.Nmesh) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Scale, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Mesh_Scale, I);
               return;
            end if;
            G.Mesh_Scale (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_pos : mjtNum (nmesh x 3)
      Count  := Int64 (S.Nmesh) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Pos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Mesh_Pos, I);
               return;
            end if;
            G.Mesh_Pos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_quat : mjtNum (nmesh x 4)
      Count  := Int64 (S.Nmesh) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Quat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Mesh_Quat, I);
               return;
            end if;
            G.Mesh_Quat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_pathadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Pathadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Pathadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polynum : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polynum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polynum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polyadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polynormal : mjtNum (nmeshpoly x 3)
      Count  := Int64 (S.Nmeshpoly) * (3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polynormal, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Mesh_Polynormal, I);
               return;
            end if;
            G.Mesh_Polynormal (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polyvertadr : int (nmeshpoly x 1)
      Count  := Int64 (S.Nmeshpoly) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polyvertadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polyvertadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polyvertnum : int (nmeshpoly x 1)
      Count  := Int64 (S.Nmeshpoly) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polyvertnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polyvertnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polyvert : int (nmeshpolyvert x 1)
      Count  := Int64 (S.Nmeshpolyvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polyvert, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polyvert (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polymapadr : int (nmeshvert x 1)
      Count  := Int64 (S.Nmeshvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polymapadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polymapadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polymapnum : int (nmeshvert x 1)
      Count  := Int64 (S.Nmeshvert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polymapnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polymapnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mesh_polymap : int (nmeshpolymap x 1)
      Count  := Int64 (S.Nmeshpolymap) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mesh_Polymap, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Mesh_Layout_OK (S, G));
         G.Mesh_Polymap (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Mesh;

   procedure Read_Skin (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Skin_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  skin_matid : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Matid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Matid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_group : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_rgba : float (nskin x 4)
      Count  := Int64 (S.Nskin) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Rgba, I);
               return;
            end if;
            G.Skin_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_inflate : float (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Inflate, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Inflate, I);
               return;
            end if;
            G.Skin_Inflate (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_vertadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Vertadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Vertadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_vertnum : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Vertnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Vertnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_texcoordadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Texcoordadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Texcoordadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_faceadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Faceadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Faceadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_facenum : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Facenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Facenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_boneadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Boneadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Boneadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonenum : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Bonenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_vert : float (nskinvert x 3)
      Count  := Int64 (S.Nskinvert) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Vert, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Vert, I);
               return;
            end if;
            G.Skin_Vert (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_texcoord : float (nskintexvert x 2)
      Count  := Int64 (S.Nskintexvert) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Texcoord, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Texcoord, I);
               return;
            end if;
            G.Skin_Texcoord (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_face : int (nskinface x 3)
      Count  := Int64 (S.Nskinface) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Face, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Face (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonevertadr : int (nskinbone x 1)
      Count  := Int64 (S.Nskinbone) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonevertadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Bonevertadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonevertnum : int (nskinbone x 1)
      Count  := Int64 (S.Nskinbone) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonevertnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Bonevertnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonebindpos : float (nskinbone x 3)
      Count  := Int64 (S.Nskinbone) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonebindpos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Bonebindpos, I);
               return;
            end if;
            G.Skin_Bonebindpos (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonebindquat : float (nskinbone x 4)
      Count  := Int64 (S.Nskinbone) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonebindquat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Bonebindquat, I);
               return;
            end if;
            G.Skin_Bonebindquat (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonebodyid : int (nskinbone x 1)
      Count  := Int64 (S.Nskinbone) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonebodyid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Bonebodyid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonevertid : int (nskinbonevert x 1)
      Count  := Int64 (S.Nskinbonevert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonevertid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Bonevertid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_bonevertweight : float (nskinbonevert x 1)
      Count  := Int64 (S.Nskinbonevert) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Bonevertweight, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Skin_Bonevertweight, I);
               return;
            end if;
            G.Skin_Bonevertweight (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  skin_pathadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Skin_Pathadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Skin_Layout_OK (S, G));
         G.Skin_Pathadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Skin;

   procedure Read_Hfield (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Hfield_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  hfield_size : mjtNum (nhfield x 4)
      Count  := Int64 (S.Nhfield) * (4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Hfield_Size, I);
               return;
            end if;
            G.Hfield_Size (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  hfield_nrow : int (nhfield x 1)
      Count  := Int64 (S.Nhfield) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Nrow, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         G.Hfield_Nrow (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  hfield_ncol : int (nhfield x 1)
      Count  := Int64 (S.Nhfield) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Ncol, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         G.Hfield_Ncol (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  hfield_adr : int (nhfield x 1)
      Count  := Int64 (S.Nhfield) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         G.Hfield_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  hfield_data : float (nhfielddata x 1)
      Count  := Int64 (S.Nhfielddata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Data, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Hfield_Data, I);
               return;
            end if;
            G.Hfield_Data (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  hfield_pathadr : int (nhfield x 1)
      Count  := Int64 (S.Nhfield) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Hfield_Pathadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Hfield_Layout_OK (S, G));
         G.Hfield_Pathadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Hfield;

   procedure Read_Texture (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Texture_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  tex_type : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_colorspace : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Colorspace, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Colorspace (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_height : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Height, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Height (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_width : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Width, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Width (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_nchannel : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Nchannel, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Nchannel (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_adr : mjtSize (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Adr (I) := Get_I64 (B, Pos + 8 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_data : mjtByte (ntexdata x 1)
      Count  := Int64 (S.Ntexdata) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Data, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Data (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tex_pathadr : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tex_Pathadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Texture_Layout_OK (S, G));
         G.Tex_Pathadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Texture;

   procedure Read_Material (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Material_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  mat_texid : int (nmat x mjNTEXROLE)
      Count  := Int64 (S.Nmat) * (10);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Texid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         G.Mat_Texid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_texuniform : mjtBool (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Texuniform, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         G.Mat_Texuniform (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_texrepeat : float (nmat x 2)
      Count  := Int64 (S.Nmat) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Texrepeat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Texrepeat, I);
               return;
            end if;
            G.Mat_Texrepeat (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_emission : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Emission, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Emission, I);
               return;
            end if;
            G.Mat_Emission (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_specular : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Specular, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Specular, I);
               return;
            end if;
            G.Mat_Specular (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_shininess : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Shininess, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Shininess, I);
               return;
            end if;
            G.Mat_Shininess (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_reflectance : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Reflectance, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Reflectance, I);
               return;
            end if;
            G.Mat_Reflectance (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_metallic : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Metallic, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Metallic, I);
               return;
            end if;
            G.Mat_Metallic (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_roughness : float (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Roughness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Roughness, I);
               return;
            end if;
            G.Mat_Roughness (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mat_rgba : float (nmat x 4)
      Count  := Int64 (S.Nmat) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Mat_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Material_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Mat_Rgba, I);
               return;
            end if;
            G.Mat_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Material;

   procedure Read_Pair (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Pair_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  pair_dim : int (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Dim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         G.Pair_Dim (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_geom1 : int (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Geom1, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         G.Pair_Geom1 (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_geom2 : int (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Geom2, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         G.Pair_Geom2 (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_signature : int (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Signature, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         G.Pair_Signature (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_solref : mjtNum (npair x mjNREF)
      Count  := Int64 (S.Npair) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Solref, I);
               return;
            end if;
            G.Pair_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_solreffriction : mjtNum (npair x mjNREF)
      Count  := Int64 (S.Npair) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Solreffriction, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Solreffriction, I);
               return;
            end if;
            G.Pair_Solreffriction (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_solimp : mjtNum (npair x mjNIMP)
      Count  := Int64 (S.Npair) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Solimp, I);
               return;
            end if;
            G.Pair_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_margin : mjtNum (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Margin, I);
               return;
            end if;
            G.Pair_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_gap : mjtNum (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Gap, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Gap, I);
               return;
            end if;
            G.Pair_Gap (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_adhesion : mjtNum (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Adhesion, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Adhesion, I);
               return;
            end if;
            G.Pair_Adhesion (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  pair_friction : mjtNum (npair x 5)
      Count  := Int64 (S.Npair) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Pair_Friction, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Pair_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Pair_Friction, I);
               return;
            end if;
            G.Pair_Friction (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Pair;

   procedure Read_Exclude (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Exclude_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  exclude_signature : int (nexclude x 1)
      Count  := Int64 (S.Nexclude) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Exclude_Signature, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Exclude_Layout_OK (S, G));
         G.Exclude_Signature (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Exclude;

   procedure Read_Equality (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Equality_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  eq_type : int (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         G.Eq_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_obj1id : int (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Obj1id, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         G.Eq_Obj1id (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_obj2id : int (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Obj2id, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         G.Eq_Obj2id (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_objtype : int (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Objtype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         G.Eq_Objtype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_active0 : mjtBool (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Active0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         G.Eq_Active0 (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_solref : mjtNum (neq x mjNREF)
      Count  := Int64 (S.Neq) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Solref, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Eq_Solref, I);
               return;
            end if;
            G.Eq_Solref (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_solimp : mjtNum (neq x mjNIMP)
      Count  := Int64 (S.Neq) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Solimp, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Eq_Solimp, I);
               return;
            end if;
            G.Eq_Solimp (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  eq_data : mjtNum (neq x mjNEQDATA)
      Count  := Int64 (S.Neq) * (11);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Eq_Data, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Equality_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Eq_Data, I);
               return;
            end if;
            G.Eq_Data (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Equality;

   procedure Read_Tendon (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Tendon_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  tendon_adr : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_num : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Num, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Num (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_matid : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Matid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Matid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_actuatorid : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Actuatorid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Actuatorid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_group : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_treenum : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Treenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Treenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_treeid : int (ntendon x 2)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Treeid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Treeid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  ten_J_rownnz : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Ten_J_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Ten_J_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  ten_J_rowadr : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Ten_J_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Ten_J_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  ten_J_colind : int (nJten x 1)
      Count  := Int64 (S.NJten) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Ten_J_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Ten_J_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_limited : mjtBool (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Limited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Limited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_actfrclimited : mjtBool (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Actfrclimited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         G.Tendon_Actfrclimited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_width : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Width, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Width, I);
               return;
            end if;
            G.Tendon_Width (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_solref_lim : mjtNum (ntendon x mjNREF)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Solref_Lim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Solref_Lim, I);
               return;
            end if;
            G.Tendon_Solref_Lim (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_solimp_lim : mjtNum (ntendon x mjNIMP)
      Count  := Int64 (S.Ntendon) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Solimp_Lim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Solimp_Lim, I);
               return;
            end if;
            G.Tendon_Solimp_Lim (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_solref_fri : mjtNum (ntendon x mjNREF)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Solref_Fri, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Solref_Fri, I);
               return;
            end if;
            G.Tendon_Solref_Fri (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_solimp_fri : mjtNum (ntendon x mjNIMP)
      Count  := Int64 (S.Ntendon) * (5);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Solimp_Fri, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Solimp_Fri, I);
               return;
            end if;
            G.Tendon_Solimp_Fri (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_range : mjtNum (ntendon x 2)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Range, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Range, I);
               return;
            end if;
            G.Tendon_Range (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_actfrcrange : mjtNum (ntendon x 2)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Actfrcrange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Actfrcrange, I);
               return;
            end if;
            G.Tendon_Actfrcrange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_margin : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Margin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Margin, I);
               return;
            end if;
            G.Tendon_Margin (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_stiffness : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Stiffness, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Stiffness, I);
               return;
            end if;
            G.Tendon_Stiffness (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_stiffnesspoly : mjtNum (ntendon x mjNPOLY)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Stiffnesspoly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Stiffnesspoly, I);
               return;
            end if;
            G.Tendon_Stiffnesspoly (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_damping : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Damping, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Damping, I);
               return;
            end if;
            G.Tendon_Damping (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_dampingpoly : mjtNum (ntendon x mjNPOLY)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Dampingpoly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Dampingpoly, I);
               return;
            end if;
            G.Tendon_Dampingpoly (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_armature : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Armature, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Armature, I);
               return;
            end if;
            G.Tendon_Armature (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_frictionloss : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Frictionloss, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Frictionloss, I);
               return;
            end if;
            G.Tendon_Frictionloss (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_lengthspring : mjtNum (ntendon x 2)
      Count  := Int64 (S.Ntendon) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Lengthspring, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Lengthspring, I);
               return;
            end if;
            G.Tendon_Lengthspring (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_length0 : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Length0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Length0, I);
               return;
            end if;
            G.Tendon_Length0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_invweight0 : mjtNum (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Invweight0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_Invweight0, I);
               return;
            end if;
            G.Tendon_Invweight0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_user : mjtNum (ntendon x MJ_M(nuser_tendon))
      Count  := Int64 (S.Ntendon) * (Int64 (S.Nuser_Tendon));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tendon_User, I);
               return;
            end if;
            G.Tendon_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tendon_rgba : float (ntendon x 4)
      Count  := Int64 (S.Ntendon) * (4);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tendon_Rgba, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tendon_Layout_OK (S, G));
         declare
            U : constant Unsigned_32 := Get_U32 (B, Pos + 4 * I);
         begin
            if not Is_Finite_F32 (U) then
               Result := (Non_Finite_Value, Tendon_Rgba, I);
               return;
            end if;
            G.Tendon_Rgba (I) := Bits_To_Float (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Tendon;

   procedure Read_Actuator (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Actuator_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  actuator_trntype : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Trntype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Trntype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_dyntype : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Dyntype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Dyntype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_gaintype : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Gaintype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Gaintype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_biastype : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Biastype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Biastype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_ctrladr : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Ctrladr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Ctrladr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_ctrlnum : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Ctrlnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Ctrlnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_ctrlspec : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Ctrlspec, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Ctrlspec (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_outadr : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Outadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Outadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_outnum : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Outnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Outnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_actadr : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Actadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Actadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_actnum : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Actnum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Actnum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_trnid : int (nactuator x 2)
      Count  := Int64 (S.Nactuator) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Trnid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Trnid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_cranklength : mjtNum (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Cranklength, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Cranklength, I);
               return;
            end if;
            G.Actuator_Cranklength (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_dynprm : mjtNum (nactuator x mjNDYN)
      Count  := Int64 (S.Nactuator) * (10);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Dynprm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Dynprm, I);
               return;
            end if;
            G.Actuator_Dynprm (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_gainprm : mjtNum (nactuator x mjNGAIN)
      Count  := Int64 (S.Nactuator) * (10);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Gainprm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Gainprm, I);
               return;
            end if;
            G.Actuator_Gainprm (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_biasprm : mjtNum (nactuator x mjNBIAS)
      Count  := Int64 (S.Nactuator) * (10);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Biasprm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Biasprm, I);
               return;
            end if;
            G.Actuator_Biasprm (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_actlimited : mjtBool (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Actlimited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Actlimited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_actrange : mjtNum (nactuator x 2)
      Count  := Int64 (S.Nactuator) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Actrange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Actrange, I);
               return;
            end if;
            G.Actuator_Actrange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_actearly : mjtBool (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Actearly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Actearly (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_history : int (nactuator x 2)
      Count  := Int64 (S.Nactuator) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_History, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_History (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_historyadr : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Historyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Historyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_delay : mjtNum (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Delay, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Delay, I);
               return;
            end if;
            G.Actuator_Delay (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_damping : mjtNum (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Damping, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Damping, I);
               return;
            end if;
            G.Actuator_Damping (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_dampingpoly : mjtNum (nactuator x mjNPOLY)
      Count  := Int64 (S.Nactuator) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Dampingpoly, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Dampingpoly, I);
               return;
            end if;
            G.Actuator_Dampingpoly (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_armature : mjtNum (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Armature, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Armature, I);
               return;
            end if;
            G.Actuator_Armature (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_group : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Group, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Group (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_user : mjtNum (nactuator x MJ_M(nuser_actuator))
      Count  := Int64 (S.Nactuator) * (Int64 (S.Nuser_Actuator));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_User, I);
               return;
            end if;
            G.Actuator_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_plugin : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Plugin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Plugin (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_forcelimited : mjtBool (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Forcelimited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Forcelimited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_forcerange : mjtNum (nactuator x 2)
      Count  := Int64 (S.Nactuator) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Forcerange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Forcerange, I);
               return;
            end if;
            G.Actuator_Forcerange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_ctrllimited : mjtBool (nu x 1)
      Count  := Int64 (S.Nu) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Ctrllimited, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         G.Actuator_Ctrllimited (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_ctrlrange : mjtNum (nu x 2)
      Count  := Int64 (S.Nu) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Ctrlrange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Ctrlrange, I);
               return;
            end if;
            G.Actuator_Ctrlrange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_gear : mjtNum (nout x 6)
      Count  := Int64 (S.Nout) * (6);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Gear, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Gear, I);
               return;
            end if;
            G.Actuator_Gear (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_acc0 : mjtNum (nout x 1)
      Count  := Int64 (S.Nout) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Acc0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Acc0, I);
               return;
            end if;
            G.Actuator_Acc0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_length0 : mjtNum (nout x 1)
      Count  := Int64 (S.Nout) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Length0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Length0, I);
               return;
            end if;
            G.Actuator_Length0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  actuator_lengthrange : mjtNum (nout x 2)
      Count  := Int64 (S.Nout) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Actuator_Lengthrange, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Actuator_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Actuator_Lengthrange, I);
               return;
            end if;
            G.Actuator_Lengthrange (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Actuator;

   procedure Read_Sensor (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Sensor_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  sensor_type : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_datatype : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Datatype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Datatype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_needstage : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Needstage, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Needstage (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_objtype : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Objtype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Objtype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_objid : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Objid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Objid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_reftype : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Reftype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Reftype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_refid : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Refid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Refid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_intprm : int (nsensor x mjNSENS)
      Count  := Int64 (S.Nsensor) * (3);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Intprm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Intprm (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_dim : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Dim, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Dim (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_adr : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_cutoff : mjtNum (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Cutoff, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Sensor_Cutoff, I);
               return;
            end if;
            G.Sensor_Cutoff (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_noise : mjtNum (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Noise, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Sensor_Noise, I);
               return;
            end if;
            G.Sensor_Noise (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_history : int (nsensor x 2)
      Count  := Int64 (S.Nsensor) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_History, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_History (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_historyadr : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Historyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Historyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_delay : mjtNum (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Delay, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Sensor_Delay, I);
               return;
            end if;
            G.Sensor_Delay (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_interval : mjtNum (nsensor x 2)
      Count  := Int64 (S.Nsensor) * (2);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Interval, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Sensor_Interval, I);
               return;
            end if;
            G.Sensor_Interval (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_user : mjtNum (nsensor x MJ_M(nuser_sensor))
      Count  := Int64 (S.Nsensor) * (Int64 (S.Nuser_Sensor));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_User, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Sensor_User, I);
               return;
            end if;
            G.Sensor_User (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  sensor_plugin : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Sensor_Plugin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sensor_Layout_OK (S, G));
         G.Sensor_Plugin (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Sensor;

   procedure Read_Qpos (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Qpos_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  qpos0 : mjtNum (nq x 1)
      Count  := Int64 (S.Nq) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Qpos0, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Qpos_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Qpos0, I);
               return;
            end if;
            G.Qpos0 (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  qpos_spring : mjtNum (nq x 1)
      Count  := Int64 (S.Nq) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Qpos_Spring, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Qpos_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Qpos_Spring, I);
               return;
            end if;
            G.Qpos_Spring (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Qpos;

   procedure Read_Bvh (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Bvh_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  bvh_depth : int (nbvh x 1)
      Count  := Int64 (S.Nbvh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Bvh_Depth, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         G.Bvh_Depth (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  bvh_child : int (nbvh x 2)
      Count  := Int64 (S.Nbvh) * (2);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Bvh_Child, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         G.Bvh_Child (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  bvh_nodeid : int (nbvh x 1)
      Count  := Int64 (S.Nbvh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Bvh_Nodeid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         G.Bvh_Nodeid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  bvh_aabb : mjtNum (nbvhstatic x 6)
      Count  := Int64 (S.Nbvhstatic) * (6);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Bvh_Aabb, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Bvh_Aabb, I);
               return;
            end if;
            G.Bvh_Aabb (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  oct_depth : int (noct x 1)
      Count  := Int64 (S.Noct) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Oct_Depth, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         G.Oct_Depth (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  oct_child : int (noct x 8)
      Count  := Int64 (S.Noct) * (8);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Oct_Child, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         G.Oct_Child (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  oct_aabb : mjtNum (noct x 6)
      Count  := Int64 (S.Noct) * (6);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Oct_Aabb, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Oct_Aabb, I);
               return;
            end if;
            G.Oct_Aabb (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  oct_coeff : mjtNum (noct x 8)
      Count  := Int64 (S.Noct) * (8);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Oct_Coeff, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Bvh_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Oct_Coeff, I);
               return;
            end if;
            G.Oct_Coeff (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Bvh;

   procedure Read_Wrap (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Wrap_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  wrap_type : int (nwrap x 1)
      Count  := Int64 (S.Nwrap) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Wrap_Type, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Wrap_Layout_OK (S, G));
         G.Wrap_Type (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  wrap_objid : int (nwrap x 1)
      Count  := Int64 (S.Nwrap) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Wrap_Objid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Wrap_Layout_OK (S, G));
         G.Wrap_Objid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  wrap_prm : mjtNum (nwrap x 1)
      Count  := Int64 (S.Nwrap) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Wrap_Prm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Wrap_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Wrap_Prm, I);
               return;
            end if;
            G.Wrap_Prm (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Wrap;

   procedure Read_Plugin (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Plugin_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  plugin : int (nplugin x 1)
      Count  := Int64 (S.Nplugin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Plugin, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Plugin_Layout_OK (S, G));
         G.Plugin (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  plugin_stateadr : int (nplugin x 1)
      Count  := Int64 (S.Nplugin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Plugin_Stateadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Plugin_Layout_OK (S, G));
         G.Plugin_Stateadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  plugin_statenum : int (nplugin x 1)
      Count  := Int64 (S.Nplugin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Plugin_Statenum, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Plugin_Layout_OK (S, G));
         G.Plugin_Statenum (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  plugin_attr : char (npluginattr x 1)
      Count  := Int64 (S.Npluginattr) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Plugin_Attr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Plugin_Layout_OK (S, G));
         G.Plugin_Attr (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  plugin_attradr : int (nplugin x 1)
      Count  := Int64 (S.Nplugin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Plugin_Attradr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Plugin_Layout_OK (S, G));
         G.Plugin_Attradr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Plugin;

   procedure Read_Numeric (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Numeric_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  numeric_adr : int (nnumeric x 1)
      Count  := Int64 (S.Nnumeric) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Numeric_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Numeric_Layout_OK (S, G));
         G.Numeric_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  numeric_size : int (nnumeric x 1)
      Count  := Int64 (S.Nnumeric) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Numeric_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Numeric_Layout_OK (S, G));
         G.Numeric_Size (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  numeric_data : mjtNum (nnumericdata x 1)
      Count  := Int64 (S.Nnumericdata) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Numeric_Data, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Numeric_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Numeric_Data, I);
               return;
            end if;
            G.Numeric_Data (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Numeric;

   procedure Read_Text (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Text_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  text_adr : int (ntext x 1)
      Count  := Int64 (S.Ntext) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Text_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Text_Layout_OK (S, G));
         G.Text_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  text_size : int (ntext x 1)
      Count  := Int64 (S.Ntext) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Text_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Text_Layout_OK (S, G));
         G.Text_Size (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  text_data : char (ntextdata x 1)
      Count  := Int64 (S.Ntextdata) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Text_Data, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Text_Layout_OK (S, G));
         G.Text_Data (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Text;

   procedure Read_Tuple (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Tuple_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  tuple_adr : int (ntuple x 1)
      Count  := Int64 (S.Ntuple) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tuple_Adr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tuple_Layout_OK (S, G));
         G.Tuple_Adr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tuple_size : int (ntuple x 1)
      Count  := Int64 (S.Ntuple) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tuple_Size, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tuple_Layout_OK (S, G));
         G.Tuple_Size (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tuple_objtype : int (ntupledata x 1)
      Count  := Int64 (S.Ntupledata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tuple_Objtype, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tuple_Layout_OK (S, G));
         G.Tuple_Objtype (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tuple_objid : int (ntupledata x 1)
      Count  := Int64 (S.Ntupledata) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tuple_Objid, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tuple_Layout_OK (S, G));
         G.Tuple_Objid (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  tuple_objprm : mjtNum (ntupledata x 1)
      Count  := Int64 (S.Ntupledata) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Tuple_Objprm, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Tuple_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Tuple_Objprm, I);
               return;
            end if;
            G.Tuple_Objprm (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Tuple;

   procedure Read_Key (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Key_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  key_time : mjtNum (nkey x 1)
      Count  := Int64 (S.Nkey) * (1);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Time, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Time, I);
               return;
            end if;
            G.Key_Time (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_qpos : mjtNum (nkey x MJ_M(nq))
      Count  := Int64 (S.Nkey) * (Int64 (S.Nq));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Qpos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Qpos, I);
               return;
            end if;
            G.Key_Qpos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_qvel : mjtNum (nkey x MJ_M(nv))
      Count  := Int64 (S.Nkey) * (Int64 (S.Nv));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Qvel, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Qvel, I);
               return;
            end if;
            G.Key_Qvel (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_act : mjtNum (nkey x MJ_M(na))
      Count  := Int64 (S.Nkey) * (Int64 (S.Na));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Act, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Act, I);
               return;
            end if;
            G.Key_Act (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_mpos : mjtNum (nkey x MJ_M(nmocap)*3)
      Count  := Int64 (S.Nkey) * (Int64 (S.Nmocap) * 3);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Mpos, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Mpos, I);
               return;
            end if;
            G.Key_Mpos (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_mquat : mjtNum (nkey x MJ_M(nmocap)*4)
      Count  := Int64 (S.Nkey) * (Int64 (S.Nmocap) * 4);
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Mquat, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Mquat, I);
               return;
            end if;
            G.Key_Mquat (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  key_ctrl : mjtNum (nkey x MJ_M(nu))
      Count  := Int64 (S.Nkey) * (Int64 (S.Nu));
      Nbytes := Count * 8;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Key_Ctrl, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Key_Layout_OK (S, G));
         declare
            U : constant Unsigned_64 := Get_U64 (B, Pos + 8 * I);
         begin
            if not Is_Finite_F64 (U) then
               Result := (Non_Finite_Value, Key_Ctrl, I);
               return;
            end if;
            G.Key_Ctrl (I) := Bits_To_Real (U);
         end;
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Key;

   procedure Read_Name (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Name_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  name_bodyadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Bodyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Bodyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_jntadr : int (njnt x 1)
      Count  := Int64 (S.Njnt) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Jntadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Jntadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_geomadr : int (ngeom x 1)
      Count  := Int64 (S.Ngeom) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Geomadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Geomadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_siteadr : int (nsite x 1)
      Count  := Int64 (S.Nsite) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Siteadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Siteadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_camadr : int (ncam x 1)
      Count  := Int64 (S.Ncam) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Camadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Camadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_lightadr : int (nlight x 1)
      Count  := Int64 (S.Nlight) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Lightadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Lightadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_flexadr : int (nflex x 1)
      Count  := Int64 (S.Nflex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Flexadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Flexadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_meshadr : int (nmesh x 1)
      Count  := Int64 (S.Nmesh) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Meshadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Meshadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_skinadr : int (nskin x 1)
      Count  := Int64 (S.Nskin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Skinadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Skinadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_hfieldadr : int (nhfield x 1)
      Count  := Int64 (S.Nhfield) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Hfieldadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Hfieldadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_texadr : int (ntex x 1)
      Count  := Int64 (S.Ntex) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Texadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Texadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_matadr : int (nmat x 1)
      Count  := Int64 (S.Nmat) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Matadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Matadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_pairadr : int (npair x 1)
      Count  := Int64 (S.Npair) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Pairadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Pairadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_excludeadr : int (nexclude x 1)
      Count  := Int64 (S.Nexclude) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Excludeadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Excludeadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_eqadr : int (neq x 1)
      Count  := Int64 (S.Neq) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Eqadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Eqadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_tendonadr : int (ntendon x 1)
      Count  := Int64 (S.Ntendon) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Tendonadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Tendonadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_actuatoradr : int (nactuator x 1)
      Count  := Int64 (S.Nactuator) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Actuatoradr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Actuatoradr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_sensoradr : int (nsensor x 1)
      Count  := Int64 (S.Nsensor) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Sensoradr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Sensoradr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_numericadr : int (nnumeric x 1)
      Count  := Int64 (S.Nnumeric) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Numericadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Numericadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_textadr : int (ntext x 1)
      Count  := Int64 (S.Ntext) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Textadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Textadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_tupleadr : int (ntuple x 1)
      Count  := Int64 (S.Ntuple) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Tupleadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Tupleadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_keyadr : int (nkey x 1)
      Count  := Int64 (S.Nkey) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Keyadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Keyadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  name_pluginadr : int (nplugin x 1)
      Count  := Int64 (S.Nplugin) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Name_Pluginadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Name_Pluginadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  names : char (nnames x 1)
      Count  := Int64 (S.Nnames) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Names, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Names (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  names_map : int (nnames_map x 1)
      Count  := Int64 (S.Nnames_Map) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Names_Map, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Names_Map (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  paths : char (npaths x 1)
      Count  := Int64 (S.Npaths) * (1);
      Nbytes := Count * 1;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, Paths, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Name_Layout_OK (S, G));
         G.Paths (I) := Get_U8 (B, Pos + I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Name;

   procedure Read_Sparse (B : Byte_Array; Pos : in out Natural; S : Sizes; G : in out Sparse_Arrays; Result : out Load_Result) is
      Count  : Int64;
      Nbytes : Int64;
   begin
      Result := OK_Result;
      --  B_rownnz : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, B_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.B_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  B_rowadr : int (nbody x 1)
      Count  := Int64 (S.Nbody) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, B_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.B_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  B_colind : int (nB x 1)
      Count  := Int64 (S.NB) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, B_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.B_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  M_rownnz : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, M_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.M_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  M_rowadr : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, M_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.M_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  M_colind : int (nC x 1)
      Count  := Int64 (S.NC) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, M_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.M_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mapM2M : int (nC x 1)
      Count  := Int64 (S.NC) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, MapM2M, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.MapM2M (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  D_rownnz : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, D_Rownnz, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.D_Rownnz (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  D_rowadr : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, D_Rowadr, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.D_Rowadr (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  D_diag : int (nv x 1)
      Count  := Int64 (S.Nv) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, D_Diag, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.D_Diag (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  D_colind : int (nD x 1)
      Count  := Int64 (S.ND) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, D_Colind, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.D_Colind (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mapM2D : int (nD x 1)
      Count  := Int64 (S.ND) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, MapM2D, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.MapM2D (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
      --  mapD2M : int (nC x 1)
      Count  := Int64 (S.NC) * (1);
      Nbytes := Count * 4;
      if Int64 (B'Length) - Int64 (Pos) < Nbytes then
         Result := (Truncated, MapD2M, -1);
         return;
      end if;
      for I in 0 .. Integer (Count) - 1 loop
         pragma Loop_Invariant (Sparse_Layout_OK (S, G));
         G.MapD2M (I) := Get_I32 (B, Pos + 4 * I);
      end loop;
      Pos := Pos + Integer (Nbytes);
   end Read_Sparse;

   procedure Read_Arrays (B : Byte_Array; Pos : in out Natural; M : in out Model; Result : out Load_Result) is
   begin
      Read_Qpos (B, Pos, M.S, M.Qpos, Result);
      if Result.Status /= OK then return; end if;
      Read_Body (B, Pos, M.S, M.Bodies, Result);
      if Result.Status /= OK then return; end if;
      Read_Bvh (B, Pos, M.S, M.Bvh, Result);
      if Result.Status /= OK then return; end if;
      Read_Joint (B, Pos, M.S, M.Joints, Result);
      if Result.Status /= OK then return; end if;
      Read_Dof (B, Pos, M.S, M.Dofs, Result);
      if Result.Status /= OK then return; end if;
      Read_Tree (B, Pos, M.S, M.Trees, Result);
      if Result.Status /= OK then return; end if;
      Read_Geom (B, Pos, M.S, M.Geoms, Result);
      if Result.Status /= OK then return; end if;
      Read_Site (B, Pos, M.S, M.Sites, Result);
      if Result.Status /= OK then return; end if;
      Read_Camera (B, Pos, M.S, M.Cameras, Result);
      if Result.Status /= OK then return; end if;
      Read_Light (B, Pos, M.S, M.Lights, Result);
      if Result.Status /= OK then return; end if;
      Read_Flex (B, Pos, M.S, M.Flexes, Result);
      if Result.Status /= OK then return; end if;
      Read_Mesh (B, Pos, M.S, M.Meshes, Result);
      if Result.Status /= OK then return; end if;
      Read_Skin (B, Pos, M.S, M.Skins, Result);
      if Result.Status /= OK then return; end if;
      Read_Hfield (B, Pos, M.S, M.Hfields, Result);
      if Result.Status /= OK then return; end if;
      Read_Texture (B, Pos, M.S, M.Textures, Result);
      if Result.Status /= OK then return; end if;
      Read_Material (B, Pos, M.S, M.Materials, Result);
      if Result.Status /= OK then return; end if;
      Read_Pair (B, Pos, M.S, M.Pairs, Result);
      if Result.Status /= OK then return; end if;
      Read_Exclude (B, Pos, M.S, M.Excludes, Result);
      if Result.Status /= OK then return; end if;
      Read_Equality (B, Pos, M.S, M.Equalities, Result);
      if Result.Status /= OK then return; end if;
      Read_Tendon (B, Pos, M.S, M.Tendons, Result);
      if Result.Status /= OK then return; end if;
      Read_Wrap (B, Pos, M.S, M.Wraps, Result);
      if Result.Status /= OK then return; end if;
      Read_Actuator (B, Pos, M.S, M.Actuators, Result);
      if Result.Status /= OK then return; end if;
      Read_Sensor (B, Pos, M.S, M.Sensors, Result);
      if Result.Status /= OK then return; end if;
      Read_Plugin (B, Pos, M.S, M.Plugins, Result);
      if Result.Status /= OK then return; end if;
      Read_Numeric (B, Pos, M.S, M.Numerics, Result);
      if Result.Status /= OK then return; end if;
      Read_Text (B, Pos, M.S, M.Texts, Result);
      if Result.Status /= OK then return; end if;
      Read_Tuple (B, Pos, M.S, M.Tuples, Result);
      if Result.Status /= OK then return; end if;
      Read_Key (B, Pos, M.S, M.Keys, Result);
      if Result.Status /= OK then return; end if;
      Read_Name (B, Pos, M.S, M.Names, Result);
      if Result.Status /= OK then return; end if;
      Read_Sparse (B, Pos, M.S, M.Sparse, Result);
      if Result.Status /= OK then return; end if;
   end Read_Arrays;

end MJ.MJB.Readers;
