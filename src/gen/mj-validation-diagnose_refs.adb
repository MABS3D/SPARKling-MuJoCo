--  GENERATED by tools/gen.py from tools/fields.txt and tools/layout.txt. Do not edit.

--  Contracts and loop invariants in this unit are proven by GNATprove and are
--  not evaluated at run time: evaluating them made the GCC optimiser inline
--  whole-model invariants at every call site (gnat1 reached 65 GB on 2026-09-03).
pragma Assertion_Policy (Pre => Ignore, Post => Ignore, Loop_Invariant => Ignore,
                         Loop_Variant => Ignore, Assert => Check);

separate (MJ.Validation)
procedure Diagnose_Refs (M : Model; Result : out Load_Result) is
   pragma Annotate (GNATprove, Hide_Info, "Expression_Function_Body", Refs_OK);
   procedure Scan_Refs_1 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Bodies.Body_Parentid /= null
       and then Input.Bodies.Body_Rootid /= null
       and then Input.Bodies.Body_Weldid /= null
       and then Input.Bodies.Body_Mocapid /= null
       and then Input.Bodies.Body_Jntadr /= null
       and then Input.Bodies.Body_Jntnum /= null
       and then Input.Bodies.Body_Jntnum'First = Input.Bodies.Body_Jntadr'First
       and then Input.Bodies.Body_Jntnum'Length = Input.Bodies.Body_Jntadr'Length
       and then Input.Bodies.Body_Dofadr /= null
       and then Input.Bodies.Body_Dofnum /= null
       and then Input.Bodies.Body_Dofnum'First = Input.Bodies.Body_Dofadr'First
       and then Input.Bodies.Body_Dofnum'Length = Input.Bodies.Body_Dofadr'Length
       and then Input.Bodies.Body_Geomadr /= null
       and then Input.Bodies.Body_Geomnum /= null
       and then Input.Bodies.Body_Geomnum'First = Input.Bodies.Body_Geomadr'First
       and then Input.Bodies.Body_Geomnum'Length = Input.Bodies.Body_Geomadr'Length
       and then Input.Bodies.Body_Bvhadr /= null
       and then Input.Bodies.Body_Bvhnum /= null
       and then Input.Bodies.Body_Bvhnum'First = Input.Bodies.Body_Bvhadr'First
       and then Input.Bodies.Body_Bvhnum'Length = Input.Bodies.Body_Bvhadr'Length;
   procedure Scan_Refs_1 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Bodies.Body_Parentid'Range loop
         if not (Ref_Body_Parentid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Parentid, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Rootid'Range loop
         if not (Ref_Body_Rootid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Rootid, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Weldid'Range loop
         if not (Ref_Body_Weldid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Weldid, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Mocapid'Range loop
         if not (Ref_Body_Mocapid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Mocapid, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Jntadr'Range loop
         if not (Ref_Body_Jntadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Jntadr, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Dofadr'Range loop
         if not (Ref_Body_Dofadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Dofadr, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Geomadr'Range loop
         if not (Ref_Body_Geomadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Geomadr, I);
            return;
         end if;
      end loop;
      for I in Input.Bodies.Body_Bvhadr'Range loop
         if not (Ref_Body_Bvhadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Bvhadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_1;

   procedure Scan_Refs_2 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Bodies.Body_Plugin /= null
       and then Input.Joints.Jnt_Qposadr /= null
       and then Input.Joints.Jnt_Dofadr /= null
       and then Input.Joints.Jnt_Bodyid /= null
       and then Input.Dofs.Dof_Bodyid /= null
       and then Input.Dofs.Dof_Jntid /= null
       and then Input.Dofs.Dof_Parentid /= null
       and then Input.Dofs.Dof_Madr /= null;
   procedure Scan_Refs_2 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Bodies.Body_Plugin'Range loop
         if not (Ref_Body_Plugin_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Body_Plugin, I);
            return;
         end if;
      end loop;
      for I in Input.Joints.Jnt_Qposadr'Range loop
         if not (Ref_Jnt_Qposadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Jnt_Qposadr, I);
            return;
         end if;
      end loop;
      for I in Input.Joints.Jnt_Dofadr'Range loop
         if not (Ref_Jnt_Dofadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Jnt_Dofadr, I);
            return;
         end if;
      end loop;
      for I in Input.Joints.Jnt_Bodyid'Range loop
         if not (Ref_Jnt_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Jnt_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Dofs.Dof_Bodyid'Range loop
         if not (Ref_Dof_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Dof_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Dofs.Dof_Jntid'Range loop
         if not (Ref_Dof_Jntid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Dof_Jntid, I);
            return;
         end if;
      end loop;
      for I in Input.Dofs.Dof_Parentid'Range loop
         if not (Ref_Dof_Parentid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Dof_Parentid, I);
            return;
         end if;
      end loop;
      for I in Input.Dofs.Dof_Madr'Range loop
         if not (Ref_Dof_Madr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Dof_Madr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_2;

   procedure Scan_Refs_3 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Trees.Tree_Bodyadr /= null
       and then Input.Trees.Tree_Bodynum /= null
       and then Input.Trees.Tree_Bodynum'First = Input.Trees.Tree_Bodyadr'First
       and then Input.Trees.Tree_Bodynum'Length = Input.Trees.Tree_Bodyadr'Length
       and then Input.Trees.Tree_Dofadr /= null
       and then Input.Trees.Tree_Dofnum /= null
       and then Input.Trees.Tree_Dofnum'First = Input.Trees.Tree_Dofadr'First
       and then Input.Trees.Tree_Dofnum'Length = Input.Trees.Tree_Dofadr'Length
       and then Input.Geoms.Geom_Bodyid /= null
       and then Input.Geoms.Geom_Matid /= null
       and then Input.Sites.Site_Bodyid /= null
       and then Input.Sites.Site_Matid /= null
       and then Input.Cameras.Cam_Bodyid /= null
       and then Input.Cameras.Cam_Targetbodyid /= null;
   procedure Scan_Refs_3 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Trees.Tree_Bodyadr'Range loop
         if not (Ref_Tree_Bodyadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tree_Bodyadr, I);
            return;
         end if;
      end loop;
      for I in Input.Trees.Tree_Dofadr'Range loop
         if not (Ref_Tree_Dofadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tree_Dofadr, I);
            return;
         end if;
      end loop;
      for I in Input.Geoms.Geom_Bodyid'Range loop
         if not (Ref_Geom_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Geom_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Geoms.Geom_Matid'Range loop
         if not (Ref_Geom_Matid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Geom_Matid, I);
            return;
         end if;
      end loop;
      for I in Input.Sites.Site_Bodyid'Range loop
         if not (Ref_Site_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Site_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Sites.Site_Matid'Range loop
         if not (Ref_Site_Matid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Site_Matid, I);
            return;
         end if;
      end loop;
      for I in Input.Cameras.Cam_Bodyid'Range loop
         if not (Ref_Cam_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Cam_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Cameras.Cam_Targetbodyid'Range loop
         if not (Ref_Cam_Targetbodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Cam_Targetbodyid, I);
            return;
         end if;
      end loop;
   end Scan_Refs_3;

   procedure Scan_Refs_4 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Lights.Light_Bodyid /= null
       and then Input.Lights.Light_Targetbodyid /= null
       and then Input.Meshes.Mesh_Vertadr /= null
       and then Input.Meshes.Mesh_Vertnum /= null
       and then Input.Meshes.Mesh_Vertnum'First = Input.Meshes.Mesh_Vertadr'First
       and then Input.Meshes.Mesh_Vertnum'Length = Input.Meshes.Mesh_Vertadr'Length
       and then Input.Meshes.Mesh_Normaladr /= null
       and then Input.Meshes.Mesh_Normalnum /= null
       and then Input.Meshes.Mesh_Normalnum'First = Input.Meshes.Mesh_Normaladr'First
       and then Input.Meshes.Mesh_Normalnum'Length = Input.Meshes.Mesh_Normaladr'Length
       and then Input.Meshes.Mesh_Texcoordadr /= null
       and then Input.Meshes.Mesh_Texcoordnum /= null
       and then Input.Meshes.Mesh_Texcoordnum'First = Input.Meshes.Mesh_Texcoordadr'First
       and then Input.Meshes.Mesh_Texcoordnum'Length = Input.Meshes.Mesh_Texcoordadr'Length
       and then Input.Meshes.Mesh_Faceadr /= null
       and then Input.Meshes.Mesh_Facenum /= null
       and then Input.Meshes.Mesh_Facenum'First = Input.Meshes.Mesh_Faceadr'First
       and then Input.Meshes.Mesh_Facenum'Length = Input.Meshes.Mesh_Faceadr'Length
       and then Input.Meshes.Mesh_Bvhadr /= null
       and then Input.Meshes.Mesh_Bvhnum /= null
       and then Input.Meshes.Mesh_Bvhnum'First = Input.Meshes.Mesh_Bvhadr'First
       and then Input.Meshes.Mesh_Bvhnum'Length = Input.Meshes.Mesh_Bvhadr'Length
       and then Input.Meshes.Mesh_Graphadr /= null;
   procedure Scan_Refs_4 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Lights.Light_Bodyid'Range loop
         if not (Ref_Light_Bodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Light_Bodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Lights.Light_Targetbodyid'Range loop
         if not (Ref_Light_Targetbodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Light_Targetbodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Vertadr'Range loop
         if not (Ref_Mesh_Vertadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Vertadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Normaladr'Range loop
         if not (Ref_Mesh_Normaladr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Normaladr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Texcoordadr'Range loop
         if not (Ref_Mesh_Texcoordadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Texcoordadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Faceadr'Range loop
         if not (Ref_Mesh_Faceadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Faceadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Bvhadr'Range loop
         if not (Ref_Mesh_Bvhadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Bvhadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Graphadr'Range loop
         if not (Ref_Mesh_Graphadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Graphadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_4;

   procedure Scan_Refs_5 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Meshes.Mesh_Polyadr /= null
       and then Input.Meshes.Mesh_Polynum /= null
       and then Input.Meshes.Mesh_Polynum'First = Input.Meshes.Mesh_Polyadr'First
       and then Input.Meshes.Mesh_Polynum'Length = Input.Meshes.Mesh_Polyadr'Length
       and then Input.Meshes.Mesh_Polyvertadr /= null
       and then Input.Meshes.Mesh_Polyvertnum /= null
       and then Input.Meshes.Mesh_Polyvertnum'First = Input.Meshes.Mesh_Polyvertadr'First
       and then Input.Meshes.Mesh_Polyvertnum'Length = Input.Meshes.Mesh_Polyvertadr'Length
       and then Input.Meshes.Mesh_Polymapadr /= null
       and then Input.Meshes.Mesh_Polymapnum /= null
       and then Input.Meshes.Mesh_Polymapnum'First = Input.Meshes.Mesh_Polymapadr'First
       and then Input.Meshes.Mesh_Polymapnum'Length = Input.Meshes.Mesh_Polymapadr'Length
       and then Input.Flexes.Flex_Vertadr /= null
       and then Input.Flexes.Flex_Vertnum /= null
       and then Input.Flexes.Flex_Vertnum'First = Input.Flexes.Flex_Vertadr'First
       and then Input.Flexes.Flex_Vertnum'Length = Input.Flexes.Flex_Vertadr'Length
       and then Input.Flexes.Flex_Edgeadr /= null
       and then Input.Flexes.Flex_Edgenum /= null
       and then Input.Flexes.Flex_Edgenum'First = Input.Flexes.Flex_Edgeadr'First
       and then Input.Flexes.Flex_Edgenum'Length = Input.Flexes.Flex_Edgeadr'Length
       and then Input.Flexes.Flex_Elemadr /= null
       and then Input.Flexes.Flex_Elemnum /= null
       and then Input.Flexes.Flex_Elemnum'First = Input.Flexes.Flex_Elemadr'First
       and then Input.Flexes.Flex_Elemnum'Length = Input.Flexes.Flex_Elemadr'Length
       and then Input.Flexes.Flex_Evpairadr /= null
       and then Input.Flexes.Flex_Evpairnum /= null
       and then Input.Flexes.Flex_Evpairnum'First = Input.Flexes.Flex_Evpairadr'First
       and then Input.Flexes.Flex_Evpairnum'Length = Input.Flexes.Flex_Evpairadr'Length
       and then Input.Flexes.Flex_Texcoordadr /= null;
   procedure Scan_Refs_5 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Meshes.Mesh_Polyadr'Range loop
         if not (Ref_Mesh_Polyadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Polyadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Polyvertadr'Range loop
         if not (Ref_Mesh_Polyvertadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Polyvertadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Polymapadr'Range loop
         if not (Ref_Mesh_Polymapadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Polymapadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Vertadr'Range loop
         if not (Ref_Flex_Vertadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Vertadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Edgeadr'Range loop
         if not (Ref_Flex_Edgeadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Edgeadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Elemadr'Range loop
         if not (Ref_Flex_Elemadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Elemadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Evpairadr'Range loop
         if not (Ref_Flex_Evpairadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Evpairadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Texcoordadr'Range loop
         if not (Ref_Flex_Texcoordadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Texcoordadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_5;

   procedure Scan_Refs_6 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Flexes.Flex_Elemdataadr /= null
       and then Input.Flexes.Flex_Elemedgeadr /= null
       and then Input.Flexes.Flex_Shelldataadr /= null
       and then Input.Flexes.Flex_Edge /= null
       and then Input.Flexes.Flex_Elem /= null
       and then Input.Flexes.Flex_Elemedge /= null
       and then Input.Flexes.Flex_Shell /= null
       and then Input.Flexes.Flex_Bvhadr /= null
       and then Input.Flexes.Flex_Bvhnum /= null
       and then Input.Flexes.Flex_Bvhnum'First = Input.Flexes.Flex_Bvhadr'First
       and then Input.Flexes.Flex_Bvhnum'Length = Input.Flexes.Flex_Bvhadr'Length;
   procedure Scan_Refs_6 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Flexes.Flex_Elemdataadr'Range loop
         if not (Ref_Flex_Elemdataadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Elemdataadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Elemedgeadr'Range loop
         if not (Ref_Flex_Elemedgeadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Elemedgeadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Shelldataadr'Range loop
         if not (Ref_Flex_Shelldataadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Shelldataadr, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Edge'Range loop
         if not (Ref_Flex_Edge_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Edge, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Elem'Range loop
         if not (Ref_Flex_Elem_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Elem, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Elemedge'Range loop
         if not (Ref_Flex_Elemedge_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Elemedge, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Shell'Range loop
         if not (Ref_Flex_Shell_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Shell, I);
            return;
         end if;
      end loop;
      for I in Input.Flexes.Flex_Bvhadr'Range loop
         if not (Ref_Flex_Bvhadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Flex_Bvhadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_6;

   procedure Scan_Refs_7 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Skins.Skin_Matid /= null
       and then Input.Skins.Skin_Vertadr /= null
       and then Input.Skins.Skin_Vertnum /= null
       and then Input.Skins.Skin_Vertnum'First = Input.Skins.Skin_Vertadr'First
       and then Input.Skins.Skin_Vertnum'Length = Input.Skins.Skin_Vertadr'Length
       and then Input.Skins.Skin_Texcoordadr /= null
       and then Input.Skins.Skin_Faceadr /= null
       and then Input.Skins.Skin_Facenum /= null
       and then Input.Skins.Skin_Facenum'First = Input.Skins.Skin_Faceadr'First
       and then Input.Skins.Skin_Facenum'Length = Input.Skins.Skin_Faceadr'Length
       and then Input.Skins.Skin_Boneadr /= null
       and then Input.Skins.Skin_Bonenum /= null
       and then Input.Skins.Skin_Bonenum'First = Input.Skins.Skin_Boneadr'First
       and then Input.Skins.Skin_Bonenum'Length = Input.Skins.Skin_Boneadr'Length
       and then Input.Skins.Skin_Bonevertadr /= null
       and then Input.Skins.Skin_Bonevertnum /= null
       and then Input.Skins.Skin_Bonevertnum'First = Input.Skins.Skin_Bonevertadr'First
       and then Input.Skins.Skin_Bonevertnum'Length = Input.Skins.Skin_Bonevertadr'Length
       and then Input.Skins.Skin_Bonebodyid /= null
       and then Input.Skins.Skin_Bonevertid /= null;
   procedure Scan_Refs_7 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Skins.Skin_Matid'Range loop
         if not (Ref_Skin_Matid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Matid, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Vertadr'Range loop
         if not (Ref_Skin_Vertadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Vertadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Texcoordadr'Range loop
         if not (Ref_Skin_Texcoordadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Texcoordadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Faceadr'Range loop
         if not (Ref_Skin_Faceadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Faceadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Boneadr'Range loop
         if not (Ref_Skin_Boneadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Boneadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Bonevertadr'Range loop
         if not (Ref_Skin_Bonevertadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Bonevertadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Bonebodyid'Range loop
         if not (Ref_Skin_Bonebodyid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Bonebodyid, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Bonevertid'Range loop
         if not (Ref_Skin_Bonevertid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Bonevertid, I);
            return;
         end if;
      end loop;
   end Scan_Refs_7;

   procedure Scan_Refs_8 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Pairs.Pair_Geom1 /= null
       and then Input.Pairs.Pair_Geom2 /= null
       and then Input.Actuators.Actuator_Plugin /= null
       and then Input.Actuators.Actuator_Actadr /= null
       and then Input.Actuators.Actuator_Actnum /= null
       and then Input.Actuators.Actuator_Actnum'First = Input.Actuators.Actuator_Actadr'First
       and then Input.Actuators.Actuator_Actnum'Length = Input.Actuators.Actuator_Actadr'Length
       and then Input.Actuators.Actuator_Ctrladr /= null
       and then Input.Actuators.Actuator_Ctrlnum /= null
       and then Input.Actuators.Actuator_Ctrlnum'First = Input.Actuators.Actuator_Ctrladr'First
       and then Input.Actuators.Actuator_Ctrlnum'Length = Input.Actuators.Actuator_Ctrladr'Length
       and then Input.Actuators.Actuator_Outadr /= null
       and then Input.Actuators.Actuator_Outnum /= null
       and then Input.Actuators.Actuator_Outnum'First = Input.Actuators.Actuator_Outadr'First
       and then Input.Actuators.Actuator_Outnum'Length = Input.Actuators.Actuator_Outadr'Length
       and then Input.Sensors.Sensor_Plugin /= null
       and then Input.Plugins.Plugin_Stateadr /= null
       and then Input.Plugins.Plugin_Statenum /= null
       and then Input.Plugins.Plugin_Statenum'First = Input.Plugins.Plugin_Stateadr'First
       and then Input.Plugins.Plugin_Statenum'Length = Input.Plugins.Plugin_Stateadr'Length;
   procedure Scan_Refs_8 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Pairs.Pair_Geom1'Range loop
         if not (Ref_Pair_Geom1_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Pair_Geom1, I);
            return;
         end if;
      end loop;
      for I in Input.Pairs.Pair_Geom2'Range loop
         if not (Ref_Pair_Geom2_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Pair_Geom2, I);
            return;
         end if;
      end loop;
      for I in Input.Actuators.Actuator_Plugin'Range loop
         if not (Ref_Actuator_Plugin_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Actuator_Plugin, I);
            return;
         end if;
      end loop;
      for I in Input.Actuators.Actuator_Actadr'Range loop
         if not (Ref_Actuator_Actadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Actuator_Actadr, I);
            return;
         end if;
      end loop;
      for I in Input.Actuators.Actuator_Ctrladr'Range loop
         if not (Ref_Actuator_Ctrladr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Actuator_Ctrladr, I);
            return;
         end if;
      end loop;
      for I in Input.Actuators.Actuator_Outadr'Range loop
         if not (Ref_Actuator_Outadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Actuator_Outadr, I);
            return;
         end if;
      end loop;
      for I in Input.Sensors.Sensor_Plugin'Range loop
         if not (Ref_Sensor_Plugin_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Sensor_Plugin, I);
            return;
         end if;
      end loop;
      for I in Input.Plugins.Plugin_Stateadr'Range loop
         if not (Ref_Plugin_Stateadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Plugin_Stateadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_8;

   procedure Scan_Refs_9 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Plugins.Plugin_Attradr /= null
       and then Input.Tendons.Tendon_Adr /= null
       and then Input.Tendons.Tendon_Num /= null
       and then Input.Tendons.Tendon_Num'First = Input.Tendons.Tendon_Adr'First
       and then Input.Tendons.Tendon_Num'Length = Input.Tendons.Tendon_Adr'Length
       and then Input.Tendons.Tendon_Matid /= null
       and then Input.Tendons.Tendon_Treeid /= null
       and then Input.Numerics.Numeric_Adr /= null
       and then Input.Numerics.Numeric_Size /= null
       and then Input.Numerics.Numeric_Size'First = Input.Numerics.Numeric_Adr'First
       and then Input.Numerics.Numeric_Size'Length = Input.Numerics.Numeric_Adr'Length
       and then Input.Texts.Text_Adr /= null
       and then Input.Texts.Text_Size /= null
       and then Input.Texts.Text_Size'First = Input.Texts.Text_Adr'First
       and then Input.Texts.Text_Size'Length = Input.Texts.Text_Adr'Length
       and then Input.Tuples.Tuple_Adr /= null
       and then Input.Tuples.Tuple_Size /= null
       and then Input.Tuples.Tuple_Size'First = Input.Tuples.Tuple_Adr'First
       and then Input.Tuples.Tuple_Size'Length = Input.Tuples.Tuple_Adr'Length
       and then Input.Names.Name_Bodyadr /= null;
   procedure Scan_Refs_9 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Plugins.Plugin_Attradr'Range loop
         if not (Ref_Plugin_Attradr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Plugin_Attradr, I);
            return;
         end if;
      end loop;
      for I in Input.Tendons.Tendon_Adr'Range loop
         if not (Ref_Tendon_Adr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tendon_Adr, I);
            return;
         end if;
      end loop;
      for I in Input.Tendons.Tendon_Matid'Range loop
         if not (Ref_Tendon_Matid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tendon_Matid, I);
            return;
         end if;
      end loop;
      for I in Input.Tendons.Tendon_Treeid'Range loop
         if not (Ref_Tendon_Treeid_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tendon_Treeid, I);
            return;
         end if;
      end loop;
      for I in Input.Numerics.Numeric_Adr'Range loop
         if not (Ref_Numeric_Adr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Numeric_Adr, I);
            return;
         end if;
      end loop;
      for I in Input.Texts.Text_Adr'Range loop
         if not (Ref_Text_Adr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Text_Adr, I);
            return;
         end if;
      end loop;
      for I in Input.Tuples.Tuple_Adr'Range loop
         if not (Ref_Tuple_Adr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tuple_Adr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Bodyadr'Range loop
         if not (Ref_Name_Bodyadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Bodyadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_9;

   procedure Scan_Refs_10 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Names.Name_Jntadr /= null
       and then Input.Names.Name_Geomadr /= null
       and then Input.Names.Name_Siteadr /= null
       and then Input.Names.Name_Camadr /= null
       and then Input.Names.Name_Lightadr /= null
       and then Input.Names.Name_Meshadr /= null
       and then Input.Names.Name_Skinadr /= null
       and then Input.Names.Name_Hfieldadr /= null;
   procedure Scan_Refs_10 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Names.Name_Jntadr'Range loop
         if not (Ref_Name_Jntadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Jntadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Geomadr'Range loop
         if not (Ref_Name_Geomadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Geomadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Siteadr'Range loop
         if not (Ref_Name_Siteadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Siteadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Camadr'Range loop
         if not (Ref_Name_Camadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Camadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Lightadr'Range loop
         if not (Ref_Name_Lightadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Lightadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Meshadr'Range loop
         if not (Ref_Name_Meshadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Meshadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Skinadr'Range loop
         if not (Ref_Name_Skinadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Skinadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Hfieldadr'Range loop
         if not (Ref_Name_Hfieldadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Hfieldadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_10;

   procedure Scan_Refs_11 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Names.Name_Texadr /= null
       and then Input.Names.Name_Matadr /= null
       and then Input.Names.Name_Pairadr /= null
       and then Input.Names.Name_Excludeadr /= null
       and then Input.Names.Name_Eqadr /= null
       and then Input.Names.Name_Tendonadr /= null
       and then Input.Names.Name_Actuatoradr /= null
       and then Input.Names.Name_Sensoradr /= null;
   procedure Scan_Refs_11 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Names.Name_Texadr'Range loop
         if not (Ref_Name_Texadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Texadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Matadr'Range loop
         if not (Ref_Name_Matadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Matadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Pairadr'Range loop
         if not (Ref_Name_Pairadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Pairadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Excludeadr'Range loop
         if not (Ref_Name_Excludeadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Excludeadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Eqadr'Range loop
         if not (Ref_Name_Eqadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Eqadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Tendonadr'Range loop
         if not (Ref_Name_Tendonadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Tendonadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Actuatoradr'Range loop
         if not (Ref_Name_Actuatoradr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Actuatoradr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Sensoradr'Range loop
         if not (Ref_Name_Sensoradr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Sensoradr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_11;

   procedure Scan_Refs_12 (Input : Model; Diagnostic : out Load_Result) with
     Pre => Input.Names.Name_Numericadr /= null
       and then Input.Names.Name_Textadr /= null
       and then Input.Names.Name_Tupleadr /= null
       and then Input.Names.Name_Keyadr /= null
       and then Input.Hfields.Hfield_Pathadr /= null
       and then Input.Meshes.Mesh_Pathadr /= null
       and then Input.Skins.Skin_Pathadr /= null
       and then Input.Textures.Tex_Pathadr /= null;
   procedure Scan_Refs_12 (Input : Model; Diagnostic : out Load_Result) is
   begin
      Diagnostic := OK_Result;
      for I in Input.Names.Name_Numericadr'Range loop
         if not (Ref_Name_Numericadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Numericadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Textadr'Range loop
         if not (Ref_Name_Textadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Textadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Tupleadr'Range loop
         if not (Ref_Name_Tupleadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Tupleadr, I);
            return;
         end if;
      end loop;
      for I in Input.Names.Name_Keyadr'Range loop
         if not (Ref_Name_Keyadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Name_Keyadr, I);
            return;
         end if;
      end loop;
      for I in Input.Hfields.Hfield_Pathadr'Range loop
         if not (Ref_Hfield_Pathadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Hfield_Pathadr, I);
            return;
         end if;
      end loop;
      for I in Input.Meshes.Mesh_Pathadr'Range loop
         if not (Ref_Mesh_Pathadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Mesh_Pathadr, I);
            return;
         end if;
      end loop;
      for I in Input.Skins.Skin_Pathadr'Range loop
         if not (Ref_Skin_Pathadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Skin_Pathadr, I);
            return;
         end if;
      end loop;
      for I in Input.Textures.Tex_Pathadr'Range loop
         if not (Ref_Tex_Pathadr_At (Input, I)) then
            Diagnostic := (Invalid_Reference, Tex_Pathadr, I);
            return;
         end if;
      end loop;
   end Scan_Refs_12;

begin
   if Refs_OK (M) then
      Result := OK_Result;
      return;
   end if;
   --  The predicate established failure; the scans locate its first field.
   Scan_Refs_1 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_2 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_3 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_4 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_5 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_6 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_7 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_8 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_9 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_10 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_11 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Scan_Refs_12 (M, Result);
   if Result.Status /= OK then
      return;
   end if;
   Result := (Invalid_Reference, Header, -1);
end Diagnose_Refs;
