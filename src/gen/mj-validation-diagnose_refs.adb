--  GENERATED by tools/gen.py from tools/fields.txt and tools/layout.txt. Do not edit.

--  Contracts and loop invariants in this unit are proven by GNATprove and are
--  not evaluated at run time: evaluating them made the GCC optimiser inline
--  whole-model invariants at every call site (gnat1 reached 65 GB on 2026-09-03).
pragma Assertion_Policy (Pre => Ignore, Post => Ignore, Loop_Invariant => Ignore,
                         Loop_Variant => Ignore, Assert => Check);

separate (MJ.Validation)
procedure Diagnose_Refs (M : Model; Result : out Load_Result) is
begin
   Result := OK_Result;
   for I in M.Bodies.Body_Parentid'Range loop
      if not (Ref_Body_Parentid_At (M, I)) then
         Result := (Invalid_Reference, Body_Parentid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Parentid'First .. I => Ref_Body_Parentid_At (M, K));
   end loop;
   for I in M.Bodies.Body_Rootid'Range loop
      if not (Ref_Body_Rootid_At (M, I)) then
         Result := (Invalid_Reference, Body_Rootid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Rootid'First .. I => Ref_Body_Rootid_At (M, K));
   end loop;
   for I in M.Bodies.Body_Weldid'Range loop
      if not (Ref_Body_Weldid_At (M, I)) then
         Result := (Invalid_Reference, Body_Weldid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Weldid'First .. I => Ref_Body_Weldid_At (M, K));
   end loop;
   for I in M.Bodies.Body_Mocapid'Range loop
      if not (Ref_Body_Mocapid_At (M, I)) then
         Result := (Invalid_Reference, Body_Mocapid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Mocapid'First .. I => Ref_Body_Mocapid_At (M, K));
   end loop;
   for I in M.Bodies.Body_Jntadr'Range loop
      if not (Ref_Body_Jntadr_At (M, I)) then
         Result := (Invalid_Reference, Body_Jntadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Jntadr'First .. I => Ref_Body_Jntadr_At (M, K));
   end loop;
   for I in M.Bodies.Body_Dofadr'Range loop
      if not (Ref_Body_Dofadr_At (M, I)) then
         Result := (Invalid_Reference, Body_Dofadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Dofadr'First .. I => Ref_Body_Dofadr_At (M, K));
   end loop;
   for I in M.Bodies.Body_Geomadr'Range loop
      if not (Ref_Body_Geomadr_At (M, I)) then
         Result := (Invalid_Reference, Body_Geomadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Geomadr'First .. I => Ref_Body_Geomadr_At (M, K));
   end loop;
   for I in M.Bodies.Body_Bvhadr'Range loop
      if not (Ref_Body_Bvhadr_At (M, I)) then
         Result := (Invalid_Reference, Body_Bvhadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Bvhadr'First .. I => Ref_Body_Bvhadr_At (M, K));
   end loop;
   for I in M.Bodies.Body_Plugin'Range loop
      if not (Ref_Body_Plugin_At (M, I)) then
         Result := (Invalid_Reference, Body_Plugin, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Bodies.Body_Plugin'First .. I => Ref_Body_Plugin_At (M, K));
   end loop;
   for I in M.Joints.Jnt_Qposadr'Range loop
      if not (Ref_Jnt_Qposadr_At (M, I)) then
         Result := (Invalid_Reference, Jnt_Qposadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Joints.Jnt_Qposadr'First .. I => Ref_Jnt_Qposadr_At (M, K));
   end loop;
   for I in M.Joints.Jnt_Dofadr'Range loop
      if not (Ref_Jnt_Dofadr_At (M, I)) then
         Result := (Invalid_Reference, Jnt_Dofadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Joints.Jnt_Dofadr'First .. I => Ref_Jnt_Dofadr_At (M, K));
   end loop;
   for I in M.Joints.Jnt_Bodyid'Range loop
      if not (Ref_Jnt_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Jnt_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Joints.Jnt_Bodyid'First .. I => Ref_Jnt_Bodyid_At (M, K));
   end loop;
   for I in M.Dofs.Dof_Bodyid'Range loop
      if not (Ref_Dof_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Dof_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Dofs.Dof_Bodyid'First .. I => Ref_Dof_Bodyid_At (M, K));
   end loop;
   for I in M.Dofs.Dof_Jntid'Range loop
      if not (Ref_Dof_Jntid_At (M, I)) then
         Result := (Invalid_Reference, Dof_Jntid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Dofs.Dof_Jntid'First .. I => Ref_Dof_Jntid_At (M, K));
   end loop;
   for I in M.Dofs.Dof_Parentid'Range loop
      if not (Ref_Dof_Parentid_At (M, I)) then
         Result := (Invalid_Reference, Dof_Parentid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Dofs.Dof_Parentid'First .. I => Ref_Dof_Parentid_At (M, K));
   end loop;
   for I in M.Dofs.Dof_Madr'Range loop
      if not (Ref_Dof_Madr_At (M, I)) then
         Result := (Invalid_Reference, Dof_Madr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Dofs.Dof_Madr'First .. I => Ref_Dof_Madr_At (M, K));
   end loop;
   for I in M.Trees.Tree_Bodyadr'Range loop
      if not (Ref_Tree_Bodyadr_At (M, I)) then
         Result := (Invalid_Reference, Tree_Bodyadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Trees.Tree_Bodyadr'First .. I => Ref_Tree_Bodyadr_At (M, K));
   end loop;
   for I in M.Trees.Tree_Dofadr'Range loop
      if not (Ref_Tree_Dofadr_At (M, I)) then
         Result := (Invalid_Reference, Tree_Dofadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Trees.Tree_Dofadr'First .. I => Ref_Tree_Dofadr_At (M, K));
   end loop;
   for I in M.Geoms.Geom_Bodyid'Range loop
      if not (Ref_Geom_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Geom_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Geoms.Geom_Bodyid'First .. I => Ref_Geom_Bodyid_At (M, K));
   end loop;
   for I in M.Geoms.Geom_Matid'Range loop
      if not (Ref_Geom_Matid_At (M, I)) then
         Result := (Invalid_Reference, Geom_Matid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Geoms.Geom_Matid'First .. I => Ref_Geom_Matid_At (M, K));
   end loop;
   for I in M.Sites.Site_Bodyid'Range loop
      if not (Ref_Site_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Site_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Sites.Site_Bodyid'First .. I => Ref_Site_Bodyid_At (M, K));
   end loop;
   for I in M.Sites.Site_Matid'Range loop
      if not (Ref_Site_Matid_At (M, I)) then
         Result := (Invalid_Reference, Site_Matid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Sites.Site_Matid'First .. I => Ref_Site_Matid_At (M, K));
   end loop;
   for I in M.Cameras.Cam_Bodyid'Range loop
      if not (Ref_Cam_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Cam_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Cameras.Cam_Bodyid'First .. I => Ref_Cam_Bodyid_At (M, K));
   end loop;
   for I in M.Cameras.Cam_Targetbodyid'Range loop
      if not (Ref_Cam_Targetbodyid_At (M, I)) then
         Result := (Invalid_Reference, Cam_Targetbodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Cameras.Cam_Targetbodyid'First .. I => Ref_Cam_Targetbodyid_At (M, K));
   end loop;
   for I in M.Lights.Light_Bodyid'Range loop
      if not (Ref_Light_Bodyid_At (M, I)) then
         Result := (Invalid_Reference, Light_Bodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Lights.Light_Bodyid'First .. I => Ref_Light_Bodyid_At (M, K));
   end loop;
   for I in M.Lights.Light_Targetbodyid'Range loop
      if not (Ref_Light_Targetbodyid_At (M, I)) then
         Result := (Invalid_Reference, Light_Targetbodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Lights.Light_Targetbodyid'First .. I => Ref_Light_Targetbodyid_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Vertadr'Range loop
      if not (Ref_Mesh_Vertadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Vertadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Vertadr'First .. I => Ref_Mesh_Vertadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Normaladr'Range loop
      if not (Ref_Mesh_Normaladr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Normaladr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Normaladr'First .. I => Ref_Mesh_Normaladr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Texcoordadr'Range loop
      if not (Ref_Mesh_Texcoordadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Texcoordadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Texcoordadr'First .. I => Ref_Mesh_Texcoordadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Faceadr'Range loop
      if not (Ref_Mesh_Faceadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Faceadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Faceadr'First .. I => Ref_Mesh_Faceadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Bvhadr'Range loop
      if not (Ref_Mesh_Bvhadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Bvhadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Bvhadr'First .. I => Ref_Mesh_Bvhadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Graphadr'Range loop
      if not (Ref_Mesh_Graphadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Graphadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Graphadr'First .. I => Ref_Mesh_Graphadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Polyadr'Range loop
      if not (Ref_Mesh_Polyadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Polyadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Polyadr'First .. I => Ref_Mesh_Polyadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Polyvertadr'Range loop
      if not (Ref_Mesh_Polyvertadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Polyvertadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Polyvertadr'First .. I => Ref_Mesh_Polyvertadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Polymapadr'Range loop
      if not (Ref_Mesh_Polymapadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Polymapadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Polymapadr'First .. I => Ref_Mesh_Polymapadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Vertadr'Range loop
      if not (Ref_Flex_Vertadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Vertadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Vertadr'First .. I => Ref_Flex_Vertadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Edgeadr'Range loop
      if not (Ref_Flex_Edgeadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Edgeadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Edgeadr'First .. I => Ref_Flex_Edgeadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Elemadr'Range loop
      if not (Ref_Flex_Elemadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Elemadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Elemadr'First .. I => Ref_Flex_Elemadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Evpairadr'Range loop
      if not (Ref_Flex_Evpairadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Evpairadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Evpairadr'First .. I => Ref_Flex_Evpairadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Texcoordadr'Range loop
      if not (Ref_Flex_Texcoordadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Texcoordadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Texcoordadr'First .. I => Ref_Flex_Texcoordadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Elemdataadr'Range loop
      if not (Ref_Flex_Elemdataadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Elemdataadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Elemdataadr'First .. I => Ref_Flex_Elemdataadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Elemedgeadr'Range loop
      if not (Ref_Flex_Elemedgeadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Elemedgeadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Elemedgeadr'First .. I => Ref_Flex_Elemedgeadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Shelldataadr'Range loop
      if not (Ref_Flex_Shelldataadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Shelldataadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Shelldataadr'First .. I => Ref_Flex_Shelldataadr_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Edge'Range loop
      if not (Ref_Flex_Edge_At (M, I)) then
         Result := (Invalid_Reference, Flex_Edge, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Edge'First .. I => Ref_Flex_Edge_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Elem'Range loop
      if not (Ref_Flex_Elem_At (M, I)) then
         Result := (Invalid_Reference, Flex_Elem, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Elem'First .. I => Ref_Flex_Elem_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Elemedge'Range loop
      if not (Ref_Flex_Elemedge_At (M, I)) then
         Result := (Invalid_Reference, Flex_Elemedge, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Elemedge'First .. I => Ref_Flex_Elemedge_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Shell'Range loop
      if not (Ref_Flex_Shell_At (M, I)) then
         Result := (Invalid_Reference, Flex_Shell, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Shell'First .. I => Ref_Flex_Shell_At (M, K));
   end loop;
   for I in M.Flexes.Flex_Bvhadr'Range loop
      if not (Ref_Flex_Bvhadr_At (M, I)) then
         Result := (Invalid_Reference, Flex_Bvhadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Flexes.Flex_Bvhadr'First .. I => Ref_Flex_Bvhadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Matid'Range loop
      if not (Ref_Skin_Matid_At (M, I)) then
         Result := (Invalid_Reference, Skin_Matid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Matid'First .. I => Ref_Skin_Matid_At (M, K));
   end loop;
   for I in M.Skins.Skin_Vertadr'Range loop
      if not (Ref_Skin_Vertadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Vertadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Vertadr'First .. I => Ref_Skin_Vertadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Texcoordadr'Range loop
      if not (Ref_Skin_Texcoordadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Texcoordadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Texcoordadr'First .. I => Ref_Skin_Texcoordadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Faceadr'Range loop
      if not (Ref_Skin_Faceadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Faceadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Faceadr'First .. I => Ref_Skin_Faceadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Boneadr'Range loop
      if not (Ref_Skin_Boneadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Boneadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Boneadr'First .. I => Ref_Skin_Boneadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Bonevertadr'Range loop
      if not (Ref_Skin_Bonevertadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Bonevertadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Bonevertadr'First .. I => Ref_Skin_Bonevertadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Bonebodyid'Range loop
      if not (Ref_Skin_Bonebodyid_At (M, I)) then
         Result := (Invalid_Reference, Skin_Bonebodyid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Bonebodyid'First .. I => Ref_Skin_Bonebodyid_At (M, K));
   end loop;
   for I in M.Skins.Skin_Bonevertid'Range loop
      if not (Ref_Skin_Bonevertid_At (M, I)) then
         Result := (Invalid_Reference, Skin_Bonevertid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Bonevertid'First .. I => Ref_Skin_Bonevertid_At (M, K));
   end loop;
   for I in M.Pairs.Pair_Geom1'Range loop
      if not (Ref_Pair_Geom1_At (M, I)) then
         Result := (Invalid_Reference, Pair_Geom1, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Pairs.Pair_Geom1'First .. I => Ref_Pair_Geom1_At (M, K));
   end loop;
   for I in M.Pairs.Pair_Geom2'Range loop
      if not (Ref_Pair_Geom2_At (M, I)) then
         Result := (Invalid_Reference, Pair_Geom2, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Pairs.Pair_Geom2'First .. I => Ref_Pair_Geom2_At (M, K));
   end loop;
   for I in M.Actuators.Actuator_Plugin'Range loop
      if not (Ref_Actuator_Plugin_At (M, I)) then
         Result := (Invalid_Reference, Actuator_Plugin, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Actuators.Actuator_Plugin'First .. I => Ref_Actuator_Plugin_At (M, K));
   end loop;
   for I in M.Actuators.Actuator_Actadr'Range loop
      if not (Ref_Actuator_Actadr_At (M, I)) then
         Result := (Invalid_Reference, Actuator_Actadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Actuators.Actuator_Actadr'First .. I => Ref_Actuator_Actadr_At (M, K));
   end loop;
   for I in M.Actuators.Actuator_Ctrladr'Range loop
      if not (Ref_Actuator_Ctrladr_At (M, I)) then
         Result := (Invalid_Reference, Actuator_Ctrladr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Actuators.Actuator_Ctrladr'First .. I => Ref_Actuator_Ctrladr_At (M, K));
   end loop;
   for I in M.Actuators.Actuator_Outadr'Range loop
      if not (Ref_Actuator_Outadr_At (M, I)) then
         Result := (Invalid_Reference, Actuator_Outadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Actuators.Actuator_Outadr'First .. I => Ref_Actuator_Outadr_At (M, K));
   end loop;
   for I in M.Sensors.Sensor_Plugin'Range loop
      if not (Ref_Sensor_Plugin_At (M, I)) then
         Result := (Invalid_Reference, Sensor_Plugin, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Sensors.Sensor_Plugin'First .. I => Ref_Sensor_Plugin_At (M, K));
   end loop;
   for I in M.Plugins.Plugin_Stateadr'Range loop
      if not (Ref_Plugin_Stateadr_At (M, I)) then
         Result := (Invalid_Reference, Plugin_Stateadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Plugins.Plugin_Stateadr'First .. I => Ref_Plugin_Stateadr_At (M, K));
   end loop;
   for I in M.Plugins.Plugin_Attradr'Range loop
      if not (Ref_Plugin_Attradr_At (M, I)) then
         Result := (Invalid_Reference, Plugin_Attradr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Plugins.Plugin_Attradr'First .. I => Ref_Plugin_Attradr_At (M, K));
   end loop;
   for I in M.Tendons.Tendon_Adr'Range loop
      if not (Ref_Tendon_Adr_At (M, I)) then
         Result := (Invalid_Reference, Tendon_Adr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Tendons.Tendon_Adr'First .. I => Ref_Tendon_Adr_At (M, K));
   end loop;
   for I in M.Tendons.Tendon_Matid'Range loop
      if not (Ref_Tendon_Matid_At (M, I)) then
         Result := (Invalid_Reference, Tendon_Matid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Tendons.Tendon_Matid'First .. I => Ref_Tendon_Matid_At (M, K));
   end loop;
   for I in M.Tendons.Tendon_Treeid'Range loop
      if not (Ref_Tendon_Treeid_At (M, I)) then
         Result := (Invalid_Reference, Tendon_Treeid, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Tendons.Tendon_Treeid'First .. I => Ref_Tendon_Treeid_At (M, K));
   end loop;
   for I in M.Numerics.Numeric_Adr'Range loop
      if not (Ref_Numeric_Adr_At (M, I)) then
         Result := (Invalid_Reference, Numeric_Adr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Numerics.Numeric_Adr'First .. I => Ref_Numeric_Adr_At (M, K));
   end loop;
   for I in M.Texts.Text_Adr'Range loop
      if not (Ref_Text_Adr_At (M, I)) then
         Result := (Invalid_Reference, Text_Adr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Texts.Text_Adr'First .. I => Ref_Text_Adr_At (M, K));
   end loop;
   for I in M.Tuples.Tuple_Adr'Range loop
      if not (Ref_Tuple_Adr_At (M, I)) then
         Result := (Invalid_Reference, Tuple_Adr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Tuples.Tuple_Adr'First .. I => Ref_Tuple_Adr_At (M, K));
   end loop;
   for I in M.Names.Name_Bodyadr'Range loop
      if not (Ref_Name_Bodyadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Bodyadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Bodyadr'First .. I => Ref_Name_Bodyadr_At (M, K));
   end loop;
   for I in M.Names.Name_Jntadr'Range loop
      if not (Ref_Name_Jntadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Jntadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Jntadr'First .. I => Ref_Name_Jntadr_At (M, K));
   end loop;
   for I in M.Names.Name_Geomadr'Range loop
      if not (Ref_Name_Geomadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Geomadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Geomadr'First .. I => Ref_Name_Geomadr_At (M, K));
   end loop;
   for I in M.Names.Name_Siteadr'Range loop
      if not (Ref_Name_Siteadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Siteadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Siteadr'First .. I => Ref_Name_Siteadr_At (M, K));
   end loop;
   for I in M.Names.Name_Camadr'Range loop
      if not (Ref_Name_Camadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Camadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Camadr'First .. I => Ref_Name_Camadr_At (M, K));
   end loop;
   for I in M.Names.Name_Lightadr'Range loop
      if not (Ref_Name_Lightadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Lightadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Lightadr'First .. I => Ref_Name_Lightadr_At (M, K));
   end loop;
   for I in M.Names.Name_Meshadr'Range loop
      if not (Ref_Name_Meshadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Meshadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Meshadr'First .. I => Ref_Name_Meshadr_At (M, K));
   end loop;
   for I in M.Names.Name_Skinadr'Range loop
      if not (Ref_Name_Skinadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Skinadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Skinadr'First .. I => Ref_Name_Skinadr_At (M, K));
   end loop;
   for I in M.Names.Name_Hfieldadr'Range loop
      if not (Ref_Name_Hfieldadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Hfieldadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Hfieldadr'First .. I => Ref_Name_Hfieldadr_At (M, K));
   end loop;
   for I in M.Names.Name_Texadr'Range loop
      if not (Ref_Name_Texadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Texadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Texadr'First .. I => Ref_Name_Texadr_At (M, K));
   end loop;
   for I in M.Names.Name_Matadr'Range loop
      if not (Ref_Name_Matadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Matadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Matadr'First .. I => Ref_Name_Matadr_At (M, K));
   end loop;
   for I in M.Names.Name_Pairadr'Range loop
      if not (Ref_Name_Pairadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Pairadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Pairadr'First .. I => Ref_Name_Pairadr_At (M, K));
   end loop;
   for I in M.Names.Name_Excludeadr'Range loop
      if not (Ref_Name_Excludeadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Excludeadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Excludeadr'First .. I => Ref_Name_Excludeadr_At (M, K));
   end loop;
   for I in M.Names.Name_Eqadr'Range loop
      if not (Ref_Name_Eqadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Eqadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Eqadr'First .. I => Ref_Name_Eqadr_At (M, K));
   end loop;
   for I in M.Names.Name_Tendonadr'Range loop
      if not (Ref_Name_Tendonadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Tendonadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Tendonadr'First .. I => Ref_Name_Tendonadr_At (M, K));
   end loop;
   for I in M.Names.Name_Actuatoradr'Range loop
      if not (Ref_Name_Actuatoradr_At (M, I)) then
         Result := (Invalid_Reference, Name_Actuatoradr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Actuatoradr'First .. I => Ref_Name_Actuatoradr_At (M, K));
   end loop;
   for I in M.Names.Name_Sensoradr'Range loop
      if not (Ref_Name_Sensoradr_At (M, I)) then
         Result := (Invalid_Reference, Name_Sensoradr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Sensoradr'First .. I => Ref_Name_Sensoradr_At (M, K));
   end loop;
   for I in M.Names.Name_Numericadr'Range loop
      if not (Ref_Name_Numericadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Numericadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Numericadr'First .. I => Ref_Name_Numericadr_At (M, K));
   end loop;
   for I in M.Names.Name_Textadr'Range loop
      if not (Ref_Name_Textadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Textadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Textadr'First .. I => Ref_Name_Textadr_At (M, K));
   end loop;
   for I in M.Names.Name_Tupleadr'Range loop
      if not (Ref_Name_Tupleadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Tupleadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Tupleadr'First .. I => Ref_Name_Tupleadr_At (M, K));
   end loop;
   for I in M.Names.Name_Keyadr'Range loop
      if not (Ref_Name_Keyadr_At (M, I)) then
         Result := (Invalid_Reference, Name_Keyadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Names.Name_Keyadr'First .. I => Ref_Name_Keyadr_At (M, K));
   end loop;
   for I in M.Hfields.Hfield_Pathadr'Range loop
      if not (Ref_Hfield_Pathadr_At (M, I)) then
         Result := (Invalid_Reference, Hfield_Pathadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Hfields.Hfield_Pathadr'First .. I => Ref_Hfield_Pathadr_At (M, K));
   end loop;
   for I in M.Meshes.Mesh_Pathadr'Range loop
      if not (Ref_Mesh_Pathadr_At (M, I)) then
         Result := (Invalid_Reference, Mesh_Pathadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Meshes.Mesh_Pathadr'First .. I => Ref_Mesh_Pathadr_At (M, K));
   end loop;
   for I in M.Skins.Skin_Pathadr'Range loop
      if not (Ref_Skin_Pathadr_At (M, I)) then
         Result := (Invalid_Reference, Skin_Pathadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Skins.Skin_Pathadr'First .. I => Ref_Skin_Pathadr_At (M, K));
   end loop;
   for I in M.Textures.Tex_Pathadr'Range loop
      if not (Ref_Tex_Pathadr_At (M, I)) then
         Result := (Invalid_Reference, Tex_Pathadr, I);
         return;
      end if;
      pragma Loop_Invariant (for all K in M.Textures.Tex_Pathadr'First .. I => Ref_Tex_Pathadr_At (M, K));
   end loop;
end Diagnose_Refs;
